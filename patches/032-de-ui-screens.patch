diff --git a/lib/ui/screens/detail_cards.dart b/lib/ui/screens/detail_cards.dart
index b8eaed7..54cad25 100644
--- a/lib/ui/screens/detail_cards.dart
+++ b/lib/ui/screens/detail_cards.dart
@@ -31,12 +31,12 @@ String _hrvDetailSummary(Map hrv) {
   final rmssd = (hrv['rmssd'] as num?)?.toDouble();
   final baseline = (hrv['baseline'] as num?)?.toDouble();
   if (rmssd == null || baseline == null || baseline <= 0) {
-    return 'SDNN, LF/HF, stability and breathing detail below.';
+    return 'SDNN, LF/HF, Stabilität und Atmungsdetails unten.';
   }
   final pct = (rmssd - baseline) / baseline;
-  if (pct.abs() < 0.08) return 'A typical HRV rhythm for you today.';
-  if (pct > 0) return 'HRV rhythm running higher than your normal today.';
-  return 'HRV rhythm running lower than your normal today.';
+  if (pct.abs() < 0.08) return 'Ein für dich typischer HRV-Rhythmus heute.';
+  if (pct > 0) return 'Dein HRV-Rhythmus liegt heute über deinem Normal.';
+  return 'Dein HRV-Rhythmus liegt heute unter deinem Normal.';
 }
 
 /// Signed deviation string for relative metrics ("+0.3", "-0.2", "0.0").
@@ -72,10 +72,17 @@ Widget _baselineRow(
   }
   if (z != null) parts.add('z ${_signed(z)}');
   final value = parts.isEmpty ? '—' : parts.join(' · ');
+  final statusLabel = switch (status) {
+    'trusted' => 'verlässlich',
+    'provisional' => 'vorläufig',
+    'calibrating' => 'lernt noch',
+    'stale' => 'veraltet',
+    _ => status,
+  };
   return DetailRow(
     label: label,
     value: value,
-    trailing: showBadge ? Tag(status, color: tagColor) : null,
+    trailing: showBadge ? Tag(statusLabel, color: tagColor) : null,
   );
 }
 
@@ -161,7 +168,7 @@ class _FetchState extends State<_Fetch> {
     }
     if (_d == null) {
       return SurfaceCard(
-        child: Text('No data', style: AppText.captionMuted),
+        child: Text('Keine Daten', style: AppText.captionMuted),
       );
     }
     return widget.build(_d!);
@@ -301,15 +308,15 @@ class HeartDayContent extends StatelessWidget {
             crossAxisAlignment: CrossAxisAlignment.start,
             children: [
               TileHeader(
-                rec != null ? 'Recovery' : 'Resting HR',
+                rec != null ? 'Erholung' : 'Ruhepuls',
                 icon: rec != null ? OsIcon.recovery : OsIcon.restingHeartRate,
                 trailing: InfoDot(
-                  title: rec != null ? 'Recovery' : 'Resting heart rate',
+                  title: rec != null ? 'Erholung' : 'Ruhepuls',
                   body: rec != null
                       ? infoFor('recovery')!
                       : infoFor('resting_hr')!,
                   methodNote: rec != null
-                      ? 'Plews lnRMSSD readiness vs your own baseline'
+                      ? 'Plews lnRMSSD vs. deine eigene Baseline'
                       : null,
                 ),
               ),
@@ -318,7 +325,7 @@ class HeartDayContent extends StatelessWidget {
                   ? BigStat(
                       value: '${rec.round()}',
                       unit: '/100',
-                      caption: 'HRV-based recovery',
+                      caption: 'HRV-basierte Erholung',
                       captionAccent: true,
                       size: BigStatSize.xl,
                       color: AppColors.scoreColor(
@@ -330,8 +337,8 @@ class HeartDayContent extends StatelessWidget {
                       unit: 'bpm',
                       caption: (rhr != null && rhrBase != null)
                           ? '${(rhr - rhrBase) >= 0 ? '+' : ''}'
-                                '${(rhr - rhrBase).toStringAsFixed(1)} vs baseline'
-                          : 'resting heart rate',
+                                '${(rhr - rhrBase).toStringAsFixed(1)} vs. Baseline'
+                          : 'Ruhepuls',
                       size: BigStatSize.xl,
                     ),
             ],
@@ -384,22 +391,22 @@ class HeartDayContent extends StatelessWidget {
               accent: DomainAccent.heart,
               onTap: dipPct == null
                   ? null
-                  : () => openTrend(context, icon: OsIcon.activity, 
-                      title: 'Nocturnal HR dip',
+                  : () => openTrend(context, icon: OsIcon.activity,
+                      title: 'Nächtlicher HF-Abfall',
                       metric: 'dip',
                       accent: DomainAccent.heart),
               child: Column(
                 crossAxisAlignment: CrossAxisAlignment.start,
                 mainAxisSize: MainAxisSize.min,
                 children: [
-                  const TileHeader('Sleeping HR'),
+                  const TileHeader('Schlaf-HF'),
                   const SizedBox(height: Sp.x2),
                   BigStat(
                     value: sleepingHr == null ? null : '${sleepingHr.round()}',
                     unit: 'bpm',
                     caption: dipPct == null
                         ? null
-                        : 'dip ${(dipPct * 100).round()}%',
+                        : 'Abfall ${(dipPct * 100).round()}%',
                     captionAccent: true,
                   ),
                 ],
@@ -414,12 +421,12 @@ class HeartDayContent extends StatelessWidget {
                 crossAxisAlignment: CrossAxisAlignment.start,
                 mainAxisSize: MainAxisSize.min,
                 children: [
-                  const TileHeader('Resting HR'),
+                  const TileHeader('Ruhepuls'),
                   const SizedBox(height: Sp.x2),
                   BigStat(
                     value: rhr == null ? null : '${rhr.round()}',
                     unit: 'bpm',
-                    caption: rhrBase == null ? null : 'usual ${rhrBase.round()}',
+                    caption: rhrBase == null ? null : 'üblich ${rhrBase.round()}',
                   ),
                 ],
               ),
@@ -436,14 +443,14 @@ class HeartDayContent extends StatelessWidget {
               crossAxisAlignment: CrossAxisAlignment.start,
               children: [
                 TileHeader(
-                  'Heart rate',
+                  'Herzfrequenz',
                   trailing: InfoDot(
-                    title: 'Heart rate',
+                    title: 'Herzfrequenz',
                     body:
-                        'Your minute-by-minute heart rate across the day, on a '
-                        'real time scale. Missing periods stay missing.',
+                        'Deine Herzfrequenz Minute für Minute über den Tag, auf '
+                        'echter Zeitskala. Fehlende Zeiträume bleiben leer.',
                     methodNote:
-                        'avg ${d['avg_hr'] ?? '—'} · max ${d['max_hr'] ?? '—'} bpm',
+                        'Ø ${d['avg_hr'] ?? '—'} · max ${d['max_hr'] ?? '—'} bpm',
                   ),
                 ),
                 const SizedBox(height: Sp.x3),
@@ -463,7 +470,7 @@ class HeartDayContent extends StatelessWidget {
           ).dsEnter(index: 2),
         ] else if (!detailedAvailable(date)) ...[
           const SizedBox(height: Sp.x3),
-          const DetailRetentionNote(what: 'minute-by-minute heart rate'),
+          const DetailRetentionNote(what: 'minutengenaue Herzfrequenz'),
         ],
 
         // ── HR zones ────────────────────────────────────────────────────────
@@ -476,11 +483,11 @@ class HeartDayContent extends StatelessWidget {
               crossAxisAlignment: CrossAxisAlignment.start,
               children: [
                 TileHeader(
-                  'Effort zones',
+                  'Belastungszonen',
                   trailing: InfoDot(
-                    title: 'Effort zones',
-                    body: 'Minutes spent in each heart-rate zone today, '
-                        'from easy (Z1) to maximal (Z5).',
+                    title: 'Belastungszonen',
+                    body: 'Minuten in jeder Herzfrequenzzone heute, '
+                        'von locker (Z1) bis maximal (Z5).',
                   ),
                 ),
                 const SizedBox(height: Sp.x3),
@@ -519,7 +526,7 @@ class HeartDayContent extends StatelessWidget {
         // ── HRV suite — full Task-Force set, each tappable into its trend ───
         if (hrv != null) ...[
           const SizedBox(height: Sp.x6),
-          const SectionHeader('Heart-rate variability'),
+          const SectionHeader('Herzfrequenzvariabilität'),
           MetricGroup([
             TrendMetricRow(
               icon: OsIcon.heartRate,
@@ -538,9 +545,9 @@ class HeartDayContent extends StatelessWidget {
               MetricRow(
                 icon: OsIcon.activity,
                 accent: AppColors.inkSoft,
-                label: 'Baseline (trend)',
-                info: 'Your typical RMSSD over recent history (simple '
-                    'trailing average) — recovery is measured against this.',
+                label: 'Baseline (Trend)',
+                info: 'Dein typischer RMSSD über die jüngere Historie (einfacher '
+                    'gleitender Durchschnitt) — daran wird die Erholung gemessen.',
                 value: '${(_n(hrv['baseline']) ?? 0).round()}',
                 unit: 'ms',
               ),
@@ -582,28 +589,28 @@ class HeartDayContent extends StatelessWidget {
                   TrendMetricRow(
                     icon: OsIcon.activity,
                     accent: DomainAccent.recovery,
-                    label: 'HRV stability',
+                    label: 'HRV-Stabilität',
                     info: infoFor('hrv_cv'),
                     value: '${hrv['cv']}',
                     unit: '%',
                     metric: 'hrv_cv',
-                    trendTitle: 'HRV stability (CV)',
+                    trendTitle: 'HRV-Stabilität (CV)',
                   ),
                 if (brvHas)
                   TrendMetricRow(
                     icon: OsIcon.activity,
                     accent: DomainAccent.recovery,
-                    label: 'Breathing variability',
-                    info: 'How much your breathing rate varied overnight '
-                        '(within-user trend), tracked against your own '
-                        'history.',
+                    label: 'Atmungsvariabilität',
+                    info: 'Wie stark deine Atemfrequenz über Nacht schwankte, '
+                        'verfolgt gegen deine eigene Historie '
+                        '(Trend innerhalb deiner Daten).',
                     value: () {
                       final cv =
                           _n((((d['brv'] as Map)['value']) as Map)['cv']);
                       return cv == null ? '—' : cv.toStringAsFixed(2);
                     }(),
                     metric: 'brv',
-                    trendTitle: 'Breathing-rate variability',
+                    trendTitle: 'Atmungsvariabilität',
                   ),
               ]),
             ),
@@ -616,15 +623,15 @@ class HeartDayContent extends StatelessWidget {
         // promise. The InfoDot below still says "screen" deliberately (the
         // clinical sense: a screening test, not a diagnosis).
         const SizedBox(height: Sp.x6),
-        const SectionHeader('Rhythm'),
+        const SectionHeader('Rhythmus'),
         Builder(builder: (context) {
           if (irr24v == null) {
             return const _QuietState(
               icon: OsIcon.heartRate,
-              title: 'Not enough clean beats today',
+              title: 'Heute nicht genug saubere Herzschläge',
               message:
-                  'The 24/7 rhythm screen needs more artifact-free beat data '
-                  'to read today.',
+                  'Das 24/7-Rhythmus-Screening braucht heute mehr artefaktfreie '
+                  'Schlagdaten für eine Auswertung.',
             );
           }
           final flag = irr24v['flag'] == true;
@@ -644,7 +651,7 @@ class HeartDayContent extends StatelessWidget {
                     const SizedBox(width: Sp.x2),
                     Expanded(
                       child: StatusChip(
-                        flag ? 'Irregular pattern today' : 'Normal',
+                        flag ? 'Unregelmäßiges Muster heute' : 'Normal',
                         icon: flag
                             ? OsIcon.activity
                             : OsIcon.check,
@@ -652,14 +659,14 @@ class HeartDayContent extends StatelessWidget {
                       ),
                     ),
                     InfoDot(
-                      title: 'Rhythm screen',
+                      title: 'Rhythmus-Screening',
                       body:
-                          "A screen, not a diagnosis — wrist pulse can't see "
-                          "the heart's electrical signal. See a clinician if "
-                          'you have symptoms (palpitations, dizziness, '
-                          'breathlessness).',
+                          'Ein Screening, keine Diagnose — der Puls am '
+                          'Handgelenk kann das elektrische Signal des Herzens '
+                          'nicht sehen. Geh bei Symptomen (Herzstolpern, '
+                          'Schwindel, Atemnot) zu Ärztin oder Arzt.',
                       methodNote:
-                          'Poincaré SD1/SD2 + pNN70 over the day’s clean beats',
+                          'Poincaré SD1/SD2 + pNN70 über die sauberen Schläge des Tages',
                     ),
                   ],
                 ),
@@ -669,8 +676,8 @@ class HeartDayContent extends StatelessWidget {
                 // acronyms are opt-in detail, collapsed by default.
                 Disclosure(
                   summary: flag
-                      ? 'Beat-timing pattern irregular — see the numbers.'
-                      : 'Beat-timing pattern normal — see the numbers.',
+                      ? 'Schlagmuster unregelmäßig — sieh dir die Zahlen an.'
+                      : 'Schlagmuster normal — sieh dir die Zahlen an.',
                   child: Row(
                     children: [
                       Expanded(
@@ -680,7 +687,7 @@ class HeartDayContent extends StatelessWidget {
                           // ratio from the raw-ms Poincaré SD1/SD2 axes shown
                           // lower on this screen's nocturnal irregular-beat
                           // card.
-                          label: 'SD1/SD2 (ratio)',
+                          label: 'SD1/SD2 (Verhältnis)',
                           size: BigStatSize.md,
                         ),
                       ),
@@ -703,16 +710,16 @@ class HeartDayContent extends StatelessWidget {
         // ── Personal baselines (Winsorized-EWMA) ─────────────────────────────
         if (baselines != null && baselines.isNotEmpty) ...[
           const SizedBox(height: Sp.x6),
-          const SectionHeader('Personal baselines'),
+          const SectionHeader('Persönliche Baselines'),
           Builder(builder: (context) {
             const rows = [
-              ['resting_hr', 'Resting HR', 'bpm', false],
+              ['resting_hr', 'Ruhepuls', 'bpm', false],
               // "(EWMA)" disambiguates this from the "Baseline (trend)" row
               // above (simple trailing mean) — same day, two legitimate
               // different numbers, now two different labels.
               ['hrv', 'HRV (RMSSD) · EWMA', 'ms', false],
-              ['resp', 'Respiratory rate', 'rpm', false],
-              ['skin_temp', 'Skin temp', '', true], // relative-only (ADC)
+              ['resp', 'Atemfrequenz', 'rpm', false],
+              ['skin_temp', 'Hauttemperatur', '', true], // relative-only (ADC)
             ];
             // Consolidated badge count: one header note instead of one pill
             // per row (was 4 TRUSTED/PROVISIONAL pills stacked vertically) —
@@ -734,21 +741,22 @@ class HeartDayContent extends StatelessWidget {
                 crossAxisAlignment: CrossAxisAlignment.start,
                 children: [
                   TileHeader(
-                    'You vs your normal',
+                    'Du vs. dein Normal',
                     trailing: InfoDot(
-                      title: 'Personal baselines',
+                      title: 'Persönliche Baselines',
                       body:
-                          'Robust, recency-weighted baselines (Winsorized '
-                          'EWMA). "z" is today vs your personal range; a '
-                          'baseline needs enough recent history before it\'s '
-                          'trusted.',
+                          'Robuste, aktualitätsgewichtete Baselines (Winsorized '
+                          'EWMA). „z“ ist heute vs. dein persönlicher Bereich; '
+                          'eine Baseline braucht genug jüngere Historie, bevor '
+                          'sie als verlässlich gilt.',
                     ),
                   ),
                   if (building > 0) ...[
                     const SizedBox(height: Sp.x1),
                     Text(
-                      '$building metric${building == 1 ? '' : 's'} still '
-                      'building baseline',
+                      building == 1
+                          ? 'Eine Metrik baut noch ihre Baseline auf'
+                          : '$building Metriken bauen noch ihre Baseline auf',
                       style: AppText.captionMuted,
                     ),
                   ],
@@ -777,12 +785,12 @@ class HeartDayContent extends StatelessWidget {
         // the Sleep tab's "Nocturnal heart" section).
         if (resp != null) ...[
           const SizedBox(height: Sp.x6),
-          const SectionHeader('Respiratory'),
+          const SectionHeader('Atmung'),
           MetricGroup([
             MetricRow(
               icon: OsIcon.activity,
               accent: DomainAccent.oxygen,
-              label: 'Respiratory rate',
+              label: 'Atemfrequenz',
               info: infoFor('resp'),
               value: '${resp['value']}',
               unit: 'brpm',
@@ -792,19 +800,19 @@ class HeartDayContent extends StatelessWidget {
 
         if (d['skin_temp'] is Map) ...[
           const SizedBox(height: Sp.x6),
-          const SectionHeader('Skin temperature'),
+          const SectionHeader('Hauttemperatur'),
           MetricGroup([
             if (d['skin_temp'] is Map &&
                 _n((d['skin_temp'] as Map)['value']) != null)
               TrendMetricRow(
                 icon: OsIcon.temperatureDeviation,
                 accent: AppColors.coralDeep,
-                label: 'Skin temp vs baseline',
+                label: 'Hauttemp. vs. Baseline',
                 info: infoFor('skin_temp'),
                 value: _signed(_n((d['skin_temp'] as Map)['value'])),
                 unit: 'Δ',
                 metric: 'skin_temp',
-                trendTitle: 'Skin temp vs baseline',
+                trendTitle: 'Hauttemp. vs. Baseline',
               )
             else
               // No value yet → honest "Need N more nights" from the
@@ -812,7 +820,7 @@ class HeartDayContent extends StatelessWidget {
               MetricRow(
                 icon: OsIcon.temperatureDeviation,
                 accent: AppColors.inkSoft,
-                label: 'Skin temp vs baseline',
+                label: 'Hauttemp. vs. Baseline',
                 info: infoFor('skin_temp'),
                 value:
                     needMessageFromNote(
@@ -825,12 +833,12 @@ class HeartDayContent extends StatelessWidget {
 
         // ── Illness watch — ALWAYS shown, three honest states ───────────────
         const SizedBox(height: Sp.x6),
-        const SectionHeader('Illness watch'),
+        const SectionHeader('Krankheits-Radar'),
         _IllnessCard(illness),
 
         // ── Irregular-beat watch (nocturnal Poincaré) — ALWAYS shown ────────
         const SizedBox(height: Sp.x6),
-        const SectionHeader('Irregular-beat watch'),
+        const SectionHeader('Radar für unregelmäßige Schläge'),
         _IrregularCard(d['irregular'] is Map ? d['irregular'] as Map : null),
       ],
     );
@@ -919,14 +927,14 @@ class OxygenNightContent extends StatelessWidget {
     final event = _eventAt(events, p.x);
     final lines = <String>[
       '${dt.hour}:$mm',
-      '${p.y.toStringAsFixed(1)}% above baseline',
-      inSleep ? 'Sleep window' : 'Outside sleep window',
+      '${p.y.toStringAsFixed(1)}% über Baseline',
+      inSleep ? 'Schlaffenster' : 'Außerhalb des Schlaffensters',
     ];
     if (event != null) {
       final peak = ((event['peak_rise_pct'] as num?)?.toDouble() ?? 0)
           .toStringAsFixed(1);
       final dur = (event['duration_sec'] as num?)?.toInt() ?? 0;
-      lines.add('Dip event · ${dur}s · peak $peak%');
+      lines.add('Abfall-Ereignis · ${dur}s · Spitze $peak%');
     }
     return lines.join('\n');
   }
@@ -1004,19 +1012,19 @@ class OxygenNightContent extends StatelessWidget {
     if (spo2 == null) {
       return const _QuietState(
         icon: OsIcon.hydration,
-        title: 'No overnight oxygen signal yet',
+        title: 'Noch kein Sauerstoffsignal über Nacht',
         message:
-            'Wear the strap through the night — the red/IR channels need a '
-            'full night of stable contact to read.',
+            'Trage das Band über Nacht — die Rot/IR-Kanäle brauchen eine ganze '
+            'Nacht stabilen Kontakt für eine Auswertung.',
       );
     }
     if (spo2['disabled'] == true) {
       return const _QuietState(
         icon: OsIcon.hydration,
-        title: 'Overnight oxygen tracking is off',
+        title: 'Sauerstoff-Tracking über Nacht ist aus',
         message:
-            'This screen is temporarily disabled pending hardware-verified '
-            'decoding — it will come back once that lands.',
+            'Dieses Screening ist vorübergehend deaktiviert, bis die '
+            'hardwareverifizierte Dekodierung fertig ist — dann kommt es zurück.',
       );
     }
 
@@ -1030,17 +1038,18 @@ class OxygenNightContent extends StatelessWidget {
             crossAxisAlignment: CrossAxisAlignment.start,
             children: [
               TileHeader(
-                'Overnight oxygen',
+                'Sauerstoff über Nacht',
                 trailing: Row(
                   mainAxisSize: MainAxisSize.min,
                   children: [
                     Tag('rel', color: accent),
                     InfoDot(
-                      title: 'Overnight oxygen',
+                      title: 'Sauerstoff über Nacht',
                       body: infoFor('spo2')!,
                       methodNote:
-                          'Relative red/IR ratio vs your own nightly baseline '
-                          '— a screening signal, never an absolute SpO₂%.',
+                          'Relatives Rot/IR-Verhältnis vs. deine eigene '
+                          'nächtliche Baseline — ein Screening-Signal, nie ein '
+                          'absolutes SpO₂ in %.',
                     ),
                   ],
                 ),
@@ -1052,9 +1061,9 @@ class OxygenNightContent extends StatelessWidget {
                   Expanded(
                     child: BigStat(
                       value: odiPerHour?.toStringAsFixed(1),
-                      unit: 'dips/h',
+                      unit: 'Abfälle/h',
                       caption:
-                          '${(spo2['dip_count'] as num?)?.toInt() ?? 0} dips · '
+                          '${(spo2['dip_count'] as num?)?.toInt() ?? 0} Abfälle · '
                           '${_nightSpan(analyzedHours)}',
                       size: BigStatSize.xl,
                     ),
@@ -1068,7 +1077,7 @@ class OxygenNightContent extends StatelessWidget {
                     valueText: signalCoverage == null
                         ? '—'
                         : '${(signalCoverage * 100).round()}%',
-                    label: 'signal',
+                    label: 'Signal',
                   ),
                 ],
               ),
@@ -1088,9 +1097,9 @@ class OxygenNightContent extends StatelessWidget {
                 mainAxisSize: MainAxisSize.min,
                 children: [
                   TileHeader(
-                    'Signal verdict',
+                    'Signal-Urteil',
                     trailing: InfoDot(
-                      title: 'Signal verdict',
+                      title: 'Signal-Urteil',
                       body: verdict.reason,
                     ),
                   ),
@@ -1099,7 +1108,7 @@ class OxygenNightContent extends StatelessWidget {
                     value: verdict.label,
                     caption: trustedCoverage == null
                         ? null
-                        : 'trusted ${(trustedCoverage * 100).round()}%',
+                        : 'verlässlich ${(trustedCoverage * 100).round()}%',
                     size: BigStatSize.md,
                     color: verdict.color,
                   ),
@@ -1112,12 +1121,12 @@ class OxygenNightContent extends StatelessWidget {
                 crossAxisAlignment: CrossAxisAlignment.start,
                 mainAxisSize: MainAxisSize.min,
                 children: [
-                  const TileHeader('Burden'),
+                  const TileHeader('Last'),
                   const SizedBox(height: Sp.x2),
                   BigStat(
                     value: burdenPct?.toStringAsFixed(1),
                     unit: '%',
-                    caption: 'of night in dips',
+                    caption: 'der Nacht in Abfällen',
                   ),
                 ],
               ),
@@ -1132,9 +1141,9 @@ class OxygenNightContent extends StatelessWidget {
                 mainAxisSize: MainAxisSize.min,
                 children: [
                   TileHeader(
-                    'Overnight load',
+                    'Nächtliche Last',
                     trailing: InfoDot(
-                      title: 'Overnight load',
+                      title: 'Nächtliche Last',
                       body: severity.reason,
                     ),
                   ),
@@ -1143,7 +1152,7 @@ class OxygenNightContent extends StatelessWidget {
                     value: severity.label,
                     caption: maxDipPct == null
                         ? null
-                        : 'strongest ${maxDipPct.toStringAsFixed(1)}%',
+                        : 'stärkster ${maxDipPct.toStringAsFixed(1)}%',
                     size: BigStatSize.md,
                     color: severity.color,
                   ),
@@ -1156,14 +1165,14 @@ class OxygenNightContent extends StatelessWidget {
                 crossAxisAlignment: CrossAxisAlignment.start,
                 mainAxisSize: MainAxisSize.min,
                 children: [
-                  const TileHeader('Longest dip'),
+                  const TileHeader('Längster Abfall'),
                   const SizedBox(height: Sp.x2),
                   BigStat(
                     value: longestDipSec == null ? null : '$longestDipSec',
                     unit: 's',
                     caption: meanDipPct == null
                         ? null
-                        : 'mean depth ${meanDipPct.toStringAsFixed(1)}%',
+                        : 'mittlere Tiefe ${meanDipPct.toStringAsFixed(1)}%',
                   ),
                 ],
               ),
@@ -1179,7 +1188,7 @@ class OxygenNightContent extends StatelessWidget {
             child: Column(
               crossAxisAlignment: CrossAxisAlignment.start,
               children: [
-                const TileHeader('Dip severity mix'),
+                const TileHeader('Schweregrade der Abfälle'),
                 const SizedBox(height: Sp.x3),
                 SegmentBar(
                   [
@@ -1196,14 +1205,14 @@ class OxygenNightContent extends StatelessWidget {
                   runSpacing: Sp.x1,
                   children: [
                     StatusChip(
-                      'Mild ${(severityCounts['mild'] as num?)?.toInt() ?? 0}',
+                      'Leicht ${(severityCounts['mild'] as num?)?.toInt() ?? 0}',
                       tone: ChipTone.positive,
                     ),
                     StatusChip(
-                      'Moderate ${(severityCounts['moderate'] as num?)?.toInt() ?? 0}',
+                      'Mittel ${(severityCounts['moderate'] as num?)?.toInt() ?? 0}',
                     ),
                     StatusChip(
-                      'Severe ${(severityCounts['severe'] as num?)?.toInt() ?? 0}',
+                      'Schwer ${(severityCounts['severe'] as num?)?.toInt() ?? 0}',
                       tone: ChipTone.warn,
                     ),
                   ],
@@ -1221,13 +1230,13 @@ class OxygenNightContent extends StatelessWidget {
             crossAxisAlignment: CrossAxisAlignment.start,
             children: [
               TileHeader(
-                'Overnight dip signal',
+                'Nächtliches Abfall-Signal',
                 trailing: InfoDot(
-                  title: 'Overnight dip signal',
+                  title: 'Nächtliches Abfall-Signal',
                   body:
-                      'Tracks overnight oxygen dips from the red/IR channel '
-                      'pair against your own nightly baseline. A screening '
-                      'signal, not an absolute saturation %.',
+                      'Verfolgt nächtliche Sauerstoff-Abfälle aus dem Rot/IR-'
+                      'Kanalpaar gegen deine eigene nächtliche Baseline. Ein '
+                      'Screening-Signal, kein absoluter Sättigungswert in %.',
                 ),
               ),
               const SizedBox(height: Sp.x3),
@@ -1264,7 +1273,7 @@ class OxygenNightContent extends StatelessWidget {
                 Padding(
                   padding: const EdgeInsets.symmetric(vertical: Sp.x4),
                   child: Text(
-                    'Not enough stable overnight signal for a dip trace yet.',
+                    'Noch nicht genug stabiles Nachtsignal für eine Abfall-Kurve.',
                     style: AppText.captionMuted,
                   ),
                 ),
@@ -1275,11 +1284,11 @@ class OxygenNightContent extends StatelessWidget {
                   runSpacing: Sp.x2,
                   children: [
                     _legendPill(
-                      'Sleep window',
+                      'Schlaffenster',
                       AppColors.cool.withValues(alpha: 0.28),
                     ),
                     _legendPill(
-                      'Detected dips',
+                      'Erkannte Abfälle',
                       AppColors.warn.withValues(alpha: 0.45),
                     ),
                   ],
@@ -1291,12 +1300,12 @@ class OxygenNightContent extends StatelessWidget {
 
         // ── the numbers ──────────────────────────────────────────────────────
         const SizedBox(height: Sp.x6),
-        const SectionHeader('The numbers'),
+        const SectionHeader('Die Zahlen'),
         MetricGroup([
           MetricRow(
             icon: OsIcon.hydration,
             accent: accent,
-            label: 'Oxygen dips',
+            label: 'Sauerstoff-Abfälle',
             info: infoFor('spo2'),
             value: odiPerHour?.toStringAsFixed(1) ?? '—',
             unit: '/h',
@@ -1304,38 +1313,38 @@ class OxygenNightContent extends StatelessWidget {
           MetricRow(
             icon: OsIcon.activity,
             accent: AppColors.warn,
-            label: 'Dip burden',
-            info: 'Share of the analyzed overnight signal spent in dip events.',
+            label: 'Abfall-Last',
+            info: 'Anteil des analysierten Nachtsignals in Abfall-Ereignissen.',
             value: burdenPct?.toStringAsFixed(1) ?? '—',
             unit: '%',
           ),
           MetricRow(
             icon: OsIcon.activity,
             accent: AppColors.good,
-            label: 'Mean dip depth',
+            label: 'Mittlere Abfalltiefe',
             info:
-                'Average size of the accepted relative dips versus the rolling '
-                'baseline.',
+                'Durchschnittliche Größe der akzeptierten relativen Abfälle '
+                'gegenüber der gleitenden Baseline.',
             value: meanDipPct?.toStringAsFixed(1) ?? '—',
             unit: '%',
           ),
           MetricRow(
             icon: OsIcon.activity,
             accent: accent,
-            label: 'Strongest dip',
+            label: 'Stärkster Abfall',
             info:
-                'Largest accepted relative dip versus the rolling nightly '
-                'baseline.',
+                'Größter akzeptierter relativer Abfall gegenüber der gleitenden '
+                'nächtlichen Baseline.',
             value: maxDipPct?.toStringAsFixed(1) ?? '—',
             unit: '%',
           ),
           MetricRow(
             icon: OsIcon.wear,
             accent: AppColors.inkSoft,
-            label: 'Signal coverage',
+            label: 'Signalabdeckung',
             info:
-                'Share of the overnight red/IR signal that was usable after '
-                'contact and stability checks.',
+                'Anteil des nächtlichen Rot/IR-Signals, der nach Kontakt- und '
+                'Stabilitätsprüfungen nutzbar war.',
             value: signalCoverage == null
                 ? '—'
                 : (signalCoverage * 100).toStringAsFixed(0),
@@ -1344,10 +1353,10 @@ class OxygenNightContent extends StatelessWidget {
           MetricRow(
             icon: OsIcon.wear,
             accent: AppColors.inkMuted,
-            label: 'Trusted coverage',
+            label: 'Verlässliche Abdeckung',
             info:
-                'Share of the overnight red/IR signal that survived the '
-                'stricter artifact gate used for dip detection.',
+                'Anteil des nächtlichen Rot/IR-Signals, der die strengere '
+                'Artefaktprüfung für die Abfall-Erkennung bestanden hat.',
             value: trustedCoverage == null
                 ? '—'
                 : (trustedCoverage * 100).toStringAsFixed(0),
@@ -1357,7 +1366,7 @@ class OxygenNightContent extends StatelessWidget {
             MetricRow(
               icon: OsIcon.activity,
               accent: AppColors.good,
-              label: 'Respiratory rate',
+              label: 'Atemfrequenz',
               info: infoFor('resp'),
               value: '${resp!['value']}',
               unit: 'brpm',
@@ -1367,7 +1376,7 @@ class OxygenNightContent extends StatelessWidget {
         // ── signal quality diagnostics ───────────────────────────────────────
         if (rejectCounts != null) ...[
           const SizedBox(height: Sp.x6),
-          const SectionHeader('Signal quality'),
+          const SectionHeader('Signalqualität'),
           SurfaceCard(
             padding: const EdgeInsets.symmetric(
               horizontal: Sp.x4,
@@ -1376,19 +1385,19 @@ class OxygenNightContent extends StatelessWidget {
             child: Column(
               children: [
                 DetailRow(
-                  label: 'Rejected: non-positive',
+                  label: 'Verworfen: nicht positiv',
                   value: '${rejectCounts['non_positive'] ?? 0}',
                 ),
                 DetailRow(
-                  label: 'Rejected: flatline',
+                  label: 'Verworfen: Flatline',
                   value: '${rejectCounts['flatline'] ?? 0}',
                 ),
                 DetailRow(
-                  label: 'Rejected: jump',
+                  label: 'Verworfen: Sprung',
                   value: '${rejectCounts['jump'] ?? 0}',
                 ),
                 DetailRow(
-                  label: 'Rejected: ratio outlier',
+                  label: 'Verworfen: Verhältnis-Ausreißer',
                   value: '${rejectCounts['ratio_outlier'] ?? 0}',
                 ),
               ],
@@ -1399,7 +1408,7 @@ class OxygenNightContent extends StatelessWidget {
         // ── detected dips list ───────────────────────────────────────────────
         if (events.isNotEmpty) ...[
           const SizedBox(height: Sp.x6),
-          const SectionHeader('Detected dips'),
+          const SectionHeader('Erkannte Abfälle'),
           SurfaceCard(
             padding: const EdgeInsets.symmetric(
               horizontal: Sp.x4,
@@ -1448,24 +1457,24 @@ Widget _legendPill(String label, Color color) {
 
   if (trusted < 0.35 || (coverage < 0.5 && rejects > 100)) {
     return (
-      label: 'Noisy',
+      label: 'Verrauscht',
       color: AppColors.warn,
-      reason: 'Low trusted coverage or too many rejected samples.',
+      reason: 'Geringe verlässliche Abdeckung oder zu viele verworfene Messwerte.',
     );
   }
   if (trusted < 0.65 || coverage < 0.75 || rejects > 500) {
     return (
-      label: 'Shaky',
+      label: 'Wackelig',
       color: AppColors.coral,
-      reason: 'Usable, but signal quality is not stable enough to fully trust.',
+      reason: 'Nutzbar, aber die Signalqualität ist nicht stabil genug, um ihr voll zu vertrauen.',
     );
   }
   return (
-    label: dips > 0 ? 'Usable' : 'Clean',
+    label: dips > 0 ? 'Nutzbar' : 'Sauber',
     color: AppColors.good,
     reason: dips > 0
-        ? 'Signal quality looks good enough to inspect the detected dips.'
-        : 'Signal quality looks good and no dips were detected.',
+        ? 'Die Signalqualität sieht gut genug aus, um die erkannten Abfälle anzusehen.'
+        : 'Die Signalqualität sieht gut aus und es wurden keine Abfälle erkannt.',
   );
 }
 
@@ -1479,10 +1488,10 @@ Widget _legendPill(String label, Color color) {
   final trusted = trustedCoverage ?? 0;
   if (trusted < 0.6) {
     return (
-      label: 'Uncertain',
+      label: 'Unsicher',
       color: AppColors.inkSoft,
       reason:
-          'Signal trust is too low to grade tonight’s oxygen burden confidently.',
+          'Das Signalvertrauen ist zu niedrig, um die nächtliche Sauerstoff-Last sicher einzustufen.',
     );
   }
 
@@ -1494,11 +1503,11 @@ Widget _legendPill(String label, Color color) {
   // live the moment SpO₂ decoding is re-enabled.
   if (odiPerHour == null && maxDipPct == null && burdenPct == null) {
     return (
-      label: 'Not graded',
+      label: 'Nicht eingestuft',
       color: AppColors.inkSoft,
       reason:
-          'The overnight dip metrics weren’t computed for this night, so '
-          'there is nothing to grade — this is not an all-clear.',
+          'Die nächtlichen Abfall-Metriken wurden für diese Nacht nicht '
+          'berechnet, es gibt also nichts einzustufen — das ist keine Entwarnung.',
     );
   }
 
@@ -1508,19 +1517,19 @@ Widget _legendPill(String label, Color color) {
 
   if (odi >= 12 || maxDip >= 8 || burden >= 6) {
     return (
-      label: 'High',
+      label: 'Hoch',
       color: AppColors.warn,
       reason:
-          'Tonight shows frequent or pronounced oxygen dips for this relative '
-          'overnight screen.',
+          'Diese Nacht zeigt häufige oder ausgeprägte Sauerstoff-Abfälle für '
+          'dieses relative Nacht-Screening.',
     );
   }
   if (odi >= 5 || maxDip >= 5 || burden >= 2) {
     return (
-      label: 'Elevated',
+      label: 'Erhöht',
       color: AppColors.coral,
       reason:
-          'Tonight has a noticeable oxygen-dip load, but not an extreme one.',
+          'Diese Nacht hat eine spürbare Abfall-Last, aber keine extreme.',
     );
   }
   // Require a repeated pattern (>=2 dips) plus a real per-hour rate/burden
@@ -1528,24 +1537,24 @@ Widget _legendPill(String label, Color color) {
   // noise, not a finding, and used to fire "Mild" on almost every night.
   if (dipCount >= 2 && (odi >= 1 || maxDip >= 2 || burden >= 0.5)) {
     return (
-      label: 'Mild',
+      label: 'Leicht',
       color: AppColors.good,
       reason:
-          'Some dips were detected, but the overall overnight burden looks '
-          'limited.',
+          'Einige Abfälle wurden erkannt, aber die nächtliche Gesamtlast sieht '
+          'begrenzt aus.',
     );
   }
   return (
-    label: 'Quiet',
+    label: 'Ruhig',
     color: AppColors.good,
-    reason: 'No meaningful overnight oxygen dips were detected in this signal.',
+    reason: 'In diesem Signal wurden keine nennenswerten nächtlichen Sauerstoff-Abfälle erkannt.',
   );
 }
 
 String _nightSpan(double? hours) {
   if (hours == null || hours <= 0) return '—';
-  if (hours < 1) return '${(hours * 60).round()} min analyzed';
-  return '${hours.toStringAsFixed(1)} h analyzed';
+  if (hours < 1) return '${(hours * 60).round()} min analysiert';
+  return '${hours.toStringAsFixed(1)} h analysiert';
 }
 
 /// Last-7-nights strip — fetches the week trend anchored on [date] and grades
@@ -1591,7 +1600,7 @@ class _OxygenRecentStripState extends State<_OxygenRecentStrip> {
   }
 
   String _label(int ts) {
-    const wd = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'];
+    const wd = ['Mo', 'Di', 'Mi', 'Do', 'Fr', 'Sa', 'So'];
     final d = DateTime.fromMillisecondsSinceEpoch(ts * 1000, isUtc: true);
     return wd[(d.weekday - 1) % 7];
   }
@@ -1601,9 +1610,9 @@ class _OxygenRecentStripState extends State<_OxygenRecentStrip> {
   ) {
     if (values.length < 2) {
       return (
-        label: 'early',
+        label: 'früh',
         color: AppColors.inkSoft,
-        reason: 'Need a few more nights before a pattern is meaningful.',
+        reason: 'Noch ein paar Nächte, bevor ein Muster aussagekräftig ist.',
       );
     }
     final latest = values.last;
@@ -1623,32 +1632,32 @@ class _OxygenRecentStripState extends State<_OxygenRecentStrip> {
 
     if (outlier) {
       return (
-        label: 'spike',
+        label: 'Ausreißer',
         color: AppColors.warn,
-        reason: 'Tonight stands well above your recent oxygen-index pattern.',
+        reason: 'Diese Nacht liegt deutlich über deinem jüngsten Sauerstoff-Index-Muster.',
       );
     }
     if (drift >= 1.0) {
       return (
-        label: 'rising',
+        label: 'steigend',
         color: AppColors.coral,
         reason:
-            'Recent nights are trending higher than the earlier part of the week.',
+            'Die letzten Nächte liegen im Trend höher als der frühere Teil der Woche.',
       );
     }
     if (drift <= -1.0) {
       return (
-        label: 'settling',
+        label: 'beruhigt sich',
         color: AppColors.good,
         reason:
-            'Recent nights are trending lower than the earlier part of the week.',
+            'Die letzten Nächte liegen im Trend niedriger als der frühere Teil der Woche.',
       );
     }
     return (
-      label: 'steady',
+      label: 'stabil',
       color: AppColors.good,
       reason:
-          'This week looks fairly stable rather than clearly rising or falling.',
+          'Diese Woche sieht eher stabil aus als klar steigend oder fallend.',
     );
   }
 
@@ -1664,8 +1673,8 @@ class _OxygenRecentStripState extends State<_OxygenRecentStrip> {
     if (present.isEmpty) {
       return const _QuietState(
         icon: OsIcon.hydration,
-        title: 'No recent oxygen trend yet',
-        message: 'A few more nights of wear build the 7-night pattern.',
+        title: 'Noch kein aktueller Sauerstoff-Trend',
+        message: 'Noch ein paar Nächte Tragen bauen das 7-Nächte-Muster auf.',
       );
     }
     final values = [
@@ -1683,22 +1692,22 @@ class _OxygenRecentStripState extends State<_OxygenRecentStrip> {
         crossAxisAlignment: CrossAxisAlignment.start,
         children: [
           TileHeader(
-            'Last 7 nights',
+            'Letzte 7 Nächte',
             trailing: Row(
               mainAxisSize: MainAxisSize.min,
               children: [
                 Tag(pattern.label, color: pattern.color),
                 InfoDot(
-                  title: 'Last 7 nights',
+                  title: 'Letzte 7 Nächte',
                   body: pattern.reason,
                   // The `spo2` SERIES is not the ODI shown in the hero above:
                   // the on-device pipeline writes null into it, so its only
                   // writer is the cloud importer's vendor saturation index.
                   // It must not be captioned "/h" as if it were a dip rate.
                   methodNote:
-                      'These bars are the imported vendor oxygen index, not '
-                      'the dip rate in the hero above and not an absolute '
-                      'SpO₂. Nights with no imported value are left blank.',
+                      'Diese Balken sind der importierte Sauerstoff-Index des '
+                      'Herstellers, nicht die Abfallrate oben und kein '
+                      'absolutes SpO₂. Nächte ohne importierten Wert bleiben leer.',
                 ),
               ],
             ),
@@ -1719,11 +1728,11 @@ class _OxygenRecentStripState extends State<_OxygenRecentStrip> {
             spacing: Sp.x2,
             runSpacing: Sp.x1,
             children: [
-              StatusChip('Latest ${latest.toStringAsFixed(1)}',
+              StatusChip('Aktuell ${latest.toStringAsFixed(1)}',
                   tone: ChipTone.accent),
-              StatusChip('Avg ${avg.toStringAsFixed(1)}'),
+              StatusChip('Ø ${avg.toStringAsFixed(1)}'),
               StatusChip(
-                '${(latest - avg >= 0 ? '+' : '')}${(latest - avg).toStringAsFixed(1)} vs avg',
+                '${(latest - avg >= 0 ? '+' : '')}${(latest - avg).toStringAsFixed(1)} vs. Ø',
               ),
             ],
           ),
@@ -1760,25 +1769,26 @@ class _IllnessCard extends StatelessWidget {
     // No baseline yet → honest "Need N more nights" state.
     if (illness == null || (cusum == null && !signal)) {
       final needLine = needNights != null
-          ? 'Need $needNights more night${needNights == 1 ? '' : 's'} of wear to start.'
-          : 'It needs about 7 nights of wear to start.';
+          ? 'Noch $needNights ${needNights == 1 ? 'Nacht' : 'Nächte'} tragen, dann geht es los.'
+          : 'Es braucht etwa 7 Nächte Tragen zum Start.';
       return _QuietState(
         icon: OsIcon.info,
         title: needNights != null
-            ? 'Need $needNights more night${needNights == 1 ? '' : 's'}'
-            : 'Building your baseline',
+            ? 'Noch $needNights ${needNights == 1 ? 'Nacht' : 'Nächte'}'
+            : 'Deine Baseline entsteht',
         message:
-            'Illness watch compares today’s resting HR, HRV and skin '
-            'temperature against your normal range. $needLine',
+            'Das Krankheits-Radar vergleicht heutigen Ruhepuls, HRV und '
+            'Hauttemperatur mit deinem Normalbereich. $needLine',
       );
     }
 
     final accent = signal ? AppColors.warn : AppColors.good;
-    final title = signal ? 'Elevated body signal' : 'All clear';
+    final title = signal ? 'Erhöhtes Körpersignal' : 'Alles in Ordnung';
     final blurb = signal
-        ? 'Your resting HR, HRV and temperature are deviating together — a '
-              'pattern that can precede illness. A signal, not a diagnosis.'
-        : 'Your resting HR, HRV and temperature are within your normal range.';
+        ? 'Dein Ruhepuls, deine HRV und deine Temperatur weichen gemeinsam ab '
+              '— ein Muster, das einer Krankheit vorausgehen kann. Ein Signal, '
+              'keine Diagnose.'
+        : 'Dein Ruhepuls, deine HRV und deine Temperatur liegen in deinem Normalbereich.';
 
     return BentoTile(
       tone: BentoTone.soft,
@@ -1800,15 +1810,15 @@ class _IllnessCard extends StatelessWidget {
               ),
               if (cusum != null)
                 Text(
-                  'index ${cusum.toStringAsFixed(1)}',
+                  'Index ${cusum.toStringAsFixed(1)}',
                   style: AppText.captionMuted,
                 ),
               InfoDot(
-                title: 'Illness watch',
+                title: 'Krankheits-Radar',
                 body: blurb,
                 methodNote:
-                    'NightSignal CUSUM over resting HR / HRV / skin temp vs '
-                    'your own baselines — a signal, not a diagnosis.',
+                    'NightSignal-CUSUM über Ruhepuls / HRV / Hauttemperatur '
+                    'vs. deine eigenen Baselines — ein Signal, keine Diagnose.',
               ),
             ],
           ),
@@ -1840,11 +1850,11 @@ class _IrregularCard extends StatelessWidget {
     if (irr == null || conf <= 0) {
       return const _QuietState(
         icon: OsIcon.info,
-        title: 'Listening for your rhythm',
+        title: 'Wir lauschen auf deinen Rhythmus',
         message:
-            'This screens your beat-to-beat (RR) timing overnight for '
-            'irregularity. It needs a night of good wear with heart-rate '
-            'variability data to read.',
+            'Das prüft über Nacht dein Schlag-zu-Schlag-Timing (RR) auf '
+            'Unregelmäßigkeiten. Es braucht eine Nacht mit gutem Sitz und '
+            'Herzfrequenzvariabilitäts-Daten für eine Auswertung.',
       );
     }
     final flag = irr!['flag'] == true;
@@ -1860,7 +1870,7 @@ class _IrregularCard extends StatelessWidget {
             children: [
               Expanded(
                 child: StatusChip(
-                  flag ? 'Irregular rhythm pattern' : 'Rhythm looks regular',
+                  flag ? 'Unregelmäßiges Rhythmusmuster' : 'Rhythmus sieht regelmäßig aus',
                   icon: flag
                       ? OsIcon.activity
                       : OsIcon.check,
@@ -1868,13 +1878,13 @@ class _IrregularCard extends StatelessWidget {
                 ),
               ),
               InfoDot(
-                title: 'Irregular-beat watch',
+                title: 'Radar für unregelmäßige Schläge',
                 body: flag
-                    ? 'Your beat-to-beat timing was unusually irregular '
-                          'overnight. A screen, not a diagnosis — if it '
-                          'persists, see a clinician.'
-                    : 'Beat-to-beat timing was within a normal range overnight.',
-                methodNote: 'Poincaré SD1/SD2 + pNN from nocturnal RR',
+                    ? 'Dein Schlag-zu-Schlag-Timing war über Nacht ungewöhnlich '
+                          'unregelmäßig. Ein Screening, keine Diagnose — wenn '
+                          'es anhält, geh zu Ärztin oder Arzt.'
+                    : 'Das Schlag-zu-Schlag-Timing lag über Nacht im normalen Bereich.',
+                methodNote: 'Poincaré SD1/SD2 + pNN aus nächtlichem RR',
               ),
             ],
           ),
@@ -1885,8 +1895,8 @@ class _IrregularCard extends StatelessWidget {
             // opt-in, collapsed by default.
             Disclosure(
               summary: flag
-                  ? 'Overnight beat-timing irregular — see the numbers.'
-                  : 'Overnight beat-timing normal — see the numbers.',
+                  ? 'Schlag-Timing über Nacht unregelmäßig — sieh dir die Zahlen an.'
+                  : 'Schlag-Timing über Nacht normal — sieh dir die Zahlen an.',
               child: Column(
                 crossAxisAlignment: CrossAxisAlignment.start,
                 children: [
@@ -1977,18 +1987,18 @@ class WearDayContent extends StatelessWidget {
     if (d.isEmpty || worn == null) {
       return const _QuietState(
         icon: OsIcon.wear,
-        title: 'Wear time wasn’t recorded',
+        title: 'Tragezeit wurde nicht aufgezeichnet',
         message:
-            'This day has no wear measurement stored — imported days don’t '
-            'carry one. That is not the same as the strap being off, so '
-            'nothing is claimed either way.',
+            'Für diesen Tag ist keine Tragezeit-Messung gespeichert — '
+            'importierte Tage haben keine. Das ist nicht dasselbe wie ein '
+            'abgelegtes Band, daher wird nichts behauptet.',
       );
     }
     if (worn == 0) {
       return const _QuietState(
         icon: OsIcon.wear,
-        title: 'Not worn on this day',
-        message: 'No wrist contact was recorded — nothing to analyze.',
+        title: 'An diesem Tag nicht getragen',
+        message: 'Kein Handgelenkkontakt aufgezeichnet — nichts zu analysieren.',
       );
     }
 
@@ -2002,17 +2012,17 @@ class WearDayContent extends StatelessWidget {
             crossAxisAlignment: CrossAxisAlignment.start,
             children: [
               TileHeader(
-                'Time worn',
+                'Getragene Zeit',
                 trailing: Row(
                   mainAxisSize: MainAxisSize.min,
                   children: [
                     Tag('AUTH', color: AppColors.good),
                     InfoDot(
-                      title: 'Wear time',
+                      title: 'Tragezeit',
                       body:
-                          'How long the strap was actually on your wrist, from '
-                          'the device wrist sensor. More wear = better sleep, '
-                          'recovery and baseline quality.',
+                          'Wie lange das Band wirklich an deinem Handgelenk '
+                          'war, vom Handgelenksensor des Geräts. Mehr Tragezeit '
+                          '= bessere Schlaf-, Erholungs- und Baseline-Qualität.',
                     ),
                   ],
                 ),
@@ -2025,8 +2035,8 @@ class WearDayContent extends StatelessWidget {
                     child: BigStat(
                       value: hm(worn),
                       caption: cov == null
-                          ? 'share of day not recorded'
-                          : '$cov% of the day',
+                          ? 'Tagesanteil nicht aufgezeichnet'
+                          : '$cov% des Tages',
                       size: BigStatSize.xl,
                     ),
                   ),
@@ -2041,7 +2051,7 @@ class WearDayContent extends StatelessWidget {
                     size: 96,
                     stroke: 10,
                     valueText: cov == null ? '—' : '$cov%',
-                    label: 'of day',
+                    label: 'des Tages',
                   ),
                 ],
               ),
@@ -2052,7 +2062,7 @@ class WearDayContent extends StatelessWidget {
         // ── 24-hour coverage strip (recent days only) ────────────────────────
         if (!detailedAvailable(date)) ...[
           const SizedBox(height: Sp.x3),
-          const DetailRetentionNote(what: 'hourly wear breakdown'),
+          const DetailRetentionNote(what: 'stundengenaue Tragezeit'),
         ] else if (hourly.length == 24) ...[
           const SizedBox(height: Sp.x3),
           SurfaceCard(
@@ -2061,10 +2071,10 @@ class WearDayContent extends StatelessWidget {
               crossAxisAlignment: CrossAxisAlignment.start,
               children: [
                 TileHeader(
-                  'Hourly coverage',
+                  'Stündliche Abdeckung',
                   trailing: InfoDot(
-                    title: 'Hourly coverage',
-                    body: 'Minutes worn in each hour of the day.',
+                    title: 'Stündliche Abdeckung',
+                    body: 'Getragene Minuten in jeder Stunde des Tages.',
                   ),
                 ),
                 const SizedBox(height: Sp.x3),
@@ -2073,11 +2083,11 @@ class WearDayContent extends StatelessWidget {
                 Row(
                   mainAxisAlignment: MainAxisAlignment.spaceBetween,
                   children: [
-                    Text('12a', style: AppText.captionMuted),
-                    Text('6a', style: AppText.captionMuted),
-                    Text('12p', style: AppText.captionMuted),
-                    Text('6p', style: AppText.captionMuted),
-                    Text('12a', style: AppText.captionMuted),
+                    Text('0 Uhr', style: AppText.captionMuted),
+                    Text('6', style: AppText.captionMuted),
+                    Text('12', style: AppText.captionMuted),
+                    Text('18', style: AppText.captionMuted),
+                    Text('24', style: AppText.captionMuted),
                   ],
                 ),
               ],
@@ -2097,9 +2107,9 @@ class WearDayContent extends StatelessWidget {
                 const SizedBox(width: Sp.x3),
                 Expanded(
                   child: Text(
-                    'Hour-by-hour wear isn’t stored for this day — only the '
-                    'day totals above are. Nothing has been estimated to '
-                    'fill the gap.',
+                    'Für diesen Tag ist keine stundengenaue Tragezeit '
+                    'gespeichert — nur die Tageswerte oben. Nichts wurde '
+                    'geschätzt, um die Lücke zu füllen.',
                     style: AppText.caption.copyWith(color: AppColors.inkSoft),
                   ),
                 ),
@@ -2118,7 +2128,7 @@ class WearDayContent extends StatelessWidget {
                 crossAxisAlignment: CrossAxisAlignment.start,
                 mainAxisSize: MainAxisSize.min,
                 children: [
-                  const TileHeader('First put on'),
+                  const TileHeader('Zuerst angelegt'),
                   const SizedBox(height: Sp.x2),
                   BigStat(value: _clock(firstOn), size: BigStatSize.md),
                 ],
@@ -2131,7 +2141,7 @@ class WearDayContent extends StatelessWidget {
                 crossAxisAlignment: CrossAxisAlignment.start,
                 mainAxisSize: MainAxisSize.min,
                 children: [
-                  const TileHeader('Wear stretches'),
+                  const TileHeader('Tragephasen'),
                   const SizedBox(height: Sp.x2),
                   // Null → BigStat's honest em-dash.
                   BigStat(
@@ -2149,7 +2159,7 @@ class WearDayContent extends StatelessWidget {
                 crossAxisAlignment: CrossAxisAlignment.start,
                 mainAxisSize: MainAxisSize.min,
                 children: [
-                  const TileHeader('Last worn'),
+                  const TileHeader('Zuletzt getragen'),
                   const SizedBox(height: Sp.x2),
                   BigStat(value: _clock(lastOn), size: BigStatSize.md),
                 ],
@@ -2162,7 +2172,7 @@ class WearDayContent extends StatelessWidget {
                 crossAxisAlignment: CrossAxisAlignment.start,
                 mainAxisSize: MainAxisSize.min,
                 children: [
-                  const TileHeader('Longest off'),
+                  const TileHeader('Längste Pause'),
                   const SizedBox(height: Sp.x2),
                   // 'none' is a positive claim ("never removed"). Only make it
                   // from a MEASURED zero — a day whose bundle has coverage but
@@ -2171,7 +2181,7 @@ class WearDayContent extends StatelessWidget {
                   BigStat(
                     value: longestOff == null
                         ? null
-                        : (longestOff > 0 ? hm(longestOff) : 'none'),
+                        : (longestOff > 0 ? hm(longestOff) : 'keine'),
                     size: BigStatSize.md,
                   ),
                 ],
@@ -2189,16 +2199,16 @@ class WearDayContent extends StatelessWidget {
 // scoped to a section, shown on its Today tab. Honest descriptive stats only.
 const _recordCfg = {
   'sleep': [
-    ('longest_sleep', 'Longest sleep', 'dur'),
-    ('best_efficiency', 'Best efficiency', 'pct'),
+    ('longest_sleep', 'Längster Schlaf', 'dur'),
+    ('best_efficiency', 'Beste Effizienz', 'pct'),
   ],
   'heart': [
-    ('lowest_rhr', 'Lowest resting HR', 'bpm'),
-    ('lowest_sleeping_hr', 'Lowest sleeping HR', 'bpm'),
+    ('lowest_rhr', 'Niedrigster Ruhepuls', 'bpm'),
+    ('lowest_sleeping_hr', 'Niedrigste Schlaf-HF', 'bpm'),
   ],
   'body': [
-    ('top_strain', 'Top strain', 'strain'),
-    ('most_steps', 'Most steps', 'int'),
+    ('top_strain', 'Höchste Belastung', 'strain'),
+    ('most_steps', 'Meiste Schritte', 'int'),
   ],
 };
 const _journalCols = {
@@ -2309,7 +2319,7 @@ class _SectionExtrasState extends State<SectionExtras> {
         final better = em['better'] == true;
         patternRows.add(
           DetailRow(
-            label: 'On "$tag" days',
+            label: 'An „$tag“-Tagen',
             value:
                 '${em['label']} ${pct >= 0 ? '+' : ''}${pct.toStringAsFixed(0)}%',
             trailing: AppIcon(
@@ -2330,12 +2340,12 @@ class _SectionExtrasState extends State<SectionExtras> {
       children: [
         if (tiles.isNotEmpty) ...[
           const SizedBox(height: Sp.x6),
-          const SectionHeader('Records'),
+          const SectionHeader('Rekorde'),
           BentoGrid(items: tiles),
         ],
         if (patternRows.isNotEmpty) ...[
           const SizedBox(height: Sp.x6),
-          const SectionHeader('Patterns'),
+          const SectionHeader('Muster'),
           SurfaceCard(
             padding: const EdgeInsets.symmetric(
               horizontal: Sp.x4,
@@ -2345,12 +2355,12 @@ class _SectionExtrasState extends State<SectionExtras> {
               crossAxisAlignment: CrossAxisAlignment.start,
               children: [
                 TileHeader(
-                  'Your tagged days',
+                  'Deine markierten Tage',
                   trailing: InfoDot(
-                    title: 'Patterns',
+                    title: 'Muster',
                     body:
-                        'How your tagged journal days compare with the rest — '
-                        'descriptive, not causal.',
+                        'Wie deine markierten Journal-Tage im Vergleich zum '
+                        'Rest abschneiden — beschreibend, nicht kausal.',
                   ),
                 ),
                 ...patternRows,
diff --git a/lib/ui/screens/metric_row.dart b/lib/ui/screens/metric_row.dart
index f14666d..c16803d 100644
--- a/lib/ui/screens/metric_row.dart
+++ b/lib/ui/screens/metric_row.dart
@@ -11,49 +11,49 @@ import '../design/design.dart';
 /// One-line, honest explanation per metric key. Lives behind the (i) so users
 /// can learn what they're looking at without the screen reading like a manual.
 const Map<String, String> kMetricInfo = {
-  'recovery': "How recovered you are — tonight's HRV vs your own baseline.",
+  'recovery': "Wie erholt du bist — die HRV dieser Nacht im Vergleich zu deiner eigenen Baseline.",
   'hrv':
-      'Beat-to-beat variability in sleep. Higher usually means better recovery.',
+      'Schlag-zu-Schlag-Variabilität im Schlaf. Höher bedeutet meist bessere Erholung.',
   'rmssd':
-      'Beat-to-beat variability in sleep. Higher usually means better recovery.',
-  'sdnn': 'Overall heart-rate variability across the night.',
-  'lf_hf': 'Balance of stress-related (LF) vs rest (HF) activity.',
-  'resting_hr': 'Your lowest heart rate while asleep — a core fitness marker.',
-  'stress': 'Sympathetic activation read from your HRV (Baevsky index).',
-  'strain': 'Cardiovascular load for the day, on a 0–21 scale.',
-  'load': 'Recent (7d) vs habitual (28d) load. 0.8–1.3 is the sweet spot.',
-  'fitness': 'Direction of your fitness from resting-HR and recovery trends.',
-  'calories': 'Active energy burned, estimated from your heart rate.',
-  'steps': 'Real steps counted by your phone or the band\'s live sensor.',
-  'sleep': 'Time actually asleep last night.',
-  'efficiency': 'Share of time in bed actually spent asleep.',
-  'regularity': 'How consistent your sleep timing is, 0–100.',
+      'Schlag-zu-Schlag-Variabilität im Schlaf. Höher bedeutet meist bessere Erholung.',
+  'sdnn': 'Gesamte Herzfrequenzvariabilität über die Nacht.',
+  'lf_hf': 'Balance aus stressbezogener (LF) und Ruhe-Aktivität (HF).',
+  'resting_hr': 'Deine niedrigste Herzfrequenz im Schlaf — ein zentraler Fitnessmarker.',
+  'stress': 'Sympathische Aktivierung, abgeleitet aus deiner HRV (Baevsky-Index).',
+  'strain': 'Kardiovaskuläre Belastung des Tages auf einer Skala von 0–21.',
+  'load': 'Aktuelle (7 Tage) vs. gewohnte (28 Tage) Belastung. 0.8–1.3 ist der Idealbereich.',
+  'fitness': 'Richtung deiner Fitness aus Ruhepuls- und Erholungstrends.',
+  'calories': 'Aktiv verbrannte Energie, geschätzt aus deiner Herzfrequenz.',
+  'steps': 'Echte Schritte, gezählt von deinem Handy oder dem Live-Sensor des Bands.',
+  'sleep': 'Tatsächliche Schlafzeit letzte Nacht.',
+  'efficiency': 'Anteil der Zeit im Bett, die du tatsächlich geschlafen hast.',
+  'regularity': 'Wie konstant deine Schlafenszeiten sind, 0–100.',
   // 4-class wrist staging (estimate): Awake / Light / Deep / REM. Light & Deep
   // split NREM via heart rate + motion (no EEG); Deep is a low-confidence overlay.
-  'light': 'Lighter non-REM sleep — the bulk of the night.',
+  'light': 'Leichterer Non-REM-Schlaf — der Großteil der Nacht.',
   'deep':
-      'Deep (slow-wave) non-REM — the body’s most restorative sleep. '
-      'A low-confidence wrist estimate.',
-  'nrem': 'Core (NREM) — non-REM sleep (Light + Deep combined).',
-  'rem': 'Dreaming sleep — mental restoration and memory.',
+      'Tiefer (Slow-Wave) Non-REM-Schlaf — der erholsamste Schlaf für den Körper. '
+      'Eine Schätzung vom Handgelenk mit geringer Sicherheit.',
+  'nrem': 'Kernschlaf (NREM) — Non-REM-Schlaf (Leicht + Tief zusammen).',
+  'rem': 'Traumschlaf — mentale Erholung und Gedächtnis.',
   'nocturnal_dip':
-      'How far your heart rate falls in sleep — a bigger dip is better.',
-  'sleeping_hr': 'Average heart rate while you slept.',
-  'resp': 'Breaths per minute, derived from heart-rate variability.',
-  'spo2': 'Relative blood-oxygen dip signal from your PPG — not an absolute reading.',
-  'skin_temp': 'Skin temperature vs your personal overnight baseline — relative, not absolute.',
+      'Wie stark deine Herzfrequenz im Schlaf absinkt — ein größerer Abfall ist besser.',
+  'sleeping_hr': 'Durchschnittliche Herzfrequenz während du geschlafen hast.',
+  'resp': 'Atemzüge pro Minute, abgeleitet aus der Herzfrequenzvariabilität.',
+  'spo2': 'Relatives Signal für Blutsauerstoff-Abfälle aus deinem PPG — kein absoluter Messwert.',
+  'skin_temp': 'Hauttemperatur im Vergleich zu deiner persönlichen Nacht-Baseline — relativ, nicht absolut.',
   'hrr60':
-      'How fast your HR drops a minute after peak effort — fitness marker.',
+      'Wie schnell deine HF eine Minute nach Spitzenbelastung abfällt — ein Fitnessmarker.',
   'illness':
-      'A combined resting-HR / HRV / temperature signal that can flag early illness.',
-  'debt': 'Sleep you owe from falling short of your need on recent nights.',
-  'hrv_cv': 'How steady your nightly HRV is — lower, stable is better.',
-  'readiness': 'A blend of HRV recovery and sleep — your day-ahead capacity.',
-  'vo2max': 'Estimated aerobic fitness from your max vs resting heart rate.',
-  'form': 'Freshness: fitness minus fatigue. Positive means well-rested.',
-  'fatigue': 'Acute training load — recent fatigue (Banister).',
-  'monotony': 'Sameness of daily strain — very high can raise injury risk.',
-  'dip': 'How far your heart rate falls in sleep — a bigger dip is better.',
+      'Ein kombiniertes Signal aus Ruhepuls, HRV und Temperatur, das früh auf Krankheit hinweisen kann.',
+  'debt': 'Schlaf, der dir fehlt, weil du in den letzten Nächten unter deinem Bedarf lagst.',
+  'hrv_cv': 'Wie stabil deine nächtliche HRV ist — niedriger und stabil ist besser.',
+  'readiness': 'Eine Mischung aus HRV-Erholung und Schlaf — deine Kapazität für den Tag.',
+  'vo2max': 'Geschätzte aerobe Fitness aus maximaler vs. Ruheherzfrequenz.',
+  'form': 'Frische: Fitness minus Ermüdung. Positiv heißt gut erholt.',
+  'fatigue': 'Akute Trainingsbelastung — kürzliche Ermüdung (Banister).',
+  'monotony': 'Gleichförmigkeit der täglichen Belastung — sehr hohe Werte können das Verletzungsrisiko erhöhen.',
+  'dip': 'Wie stark deine Herzfrequenz im Schlaf absinkt — ein größerer Abfall ist besser.',
 };
 
 String? infoFor(String key) => kMetricInfo[key];
diff --git a/lib/ui/screens/metric_screen.dart b/lib/ui/screens/metric_screen.dart
index b07a8d5..32c488c 100644
--- a/lib/ui/screens/metric_screen.dart
+++ b/lib/ui/screens/metric_screen.dart
@@ -17,9 +17,9 @@ import 'metric_row.dart' show infoFor;
 typedef DetailBuilder = Widget Function(BuildContext context);
 typedef DayDetailBuilder = Widget Function(BuildContext context, String date);
 
-const _tabs = ['Today', 'Week', 'Month', '3M'];
-const _wd = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'];
-const _mon = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
+const _tabs = ['Heute', 'Woche', 'Monat', '3M'];
+const _wd = ['Mo', 'Di', 'Mi', 'Do', 'Fr', 'Sa', 'So'];
+const _mon = ['Jan', 'Feb', 'Mär', 'Apr', 'Mai', 'Jun', 'Jul', 'Aug', 'Sep', 'Okt', 'Nov', 'Dez'];
 
 class MetricScreen extends StatefulWidget {
   final String title;
@@ -180,7 +180,7 @@ class _DrillLevelState extends State<_DrillLevel> {
       if (widget.scale == 'week') {
         final d = DateTime.fromMillisecondsSinceEpoch(
             (endTs - 86400) * 1000, isUtc: true);
-        _childLabel = '${_wd[(d.weekday - 1) % 7]}, ${_mon[d.month - 1]} ${d.day}';
+        _childLabel = '${_wd[(d.weekday - 1) % 7]}, ${d.day}. ${_mon[d.month - 1]}';
         _child = KeyedSubtree(
             key: ValueKey('day-$lastDay'),
             child: widget.dayDetail(context, lastDay));
@@ -242,7 +242,7 @@ String trendBarLabel(String scale, int i, Map b) {
       return _wd[(d.weekday - 1) % 7];
     case 'month':
       final end = trendBucketEnd(b) ?? d.add(const Duration(days: 6));
-      return '${_mon[end.month - 1]} ${end.day}';
+      return '${end.day}. ${_mon[end.month - 1]}';
     default: // quarter → month
       return _mon[d.month - 1];
   }
@@ -283,7 +283,7 @@ double? trendBucketValue(Map b) {
   String unit,
 ) {
   if (metric == 'spo2') {
-    return (label: 'imported oxygen index', unit: '');
+    return (label: 'importierter Sauerstoff-Index', unit: '');
   }
   return (label: label, unit: unit);
 }
@@ -328,10 +328,10 @@ class TrendBoard extends StatelessWidget {
   /// so after five unsynced days "this week" is a fortnight ago. [_periodOf]
   /// prefers the buckets' real dates; this is only the fallback.
   String get _period => scale == 'week'
-      ? '7 days'
+      ? '7 Tage'
       : scale == 'month'
-      ? '4 weeks'
-      : '3 months';
+      ? '4 Wochen'
+      : '3 Monate';
 
   /// The window the bars ACTUALLY cover, read off the buckets ('Jul 15–21').
   String _periodOf(List buckets) {
@@ -343,10 +343,10 @@ class TrendBoard extends StatelessWidget {
       firstTs * 1000,
       isUtc: true,
     );
-    final s = '${_mon[start.month - 1]} ${start.day}';
-    final e = start.month == last.month
-        ? '${last.day}'
-        : '${_mon[last.month - 1]} ${last.day}';
+    final s = start.month == last.month
+        ? '${start.day}.'
+        : '${start.day}. ${_mon[start.month - 1]}';
+    final e = '${last.day}. ${_mon[last.month - 1]}';
     return '$s–$e';
   }
 
@@ -410,22 +410,22 @@ class TrendBoard extends StatelessWidget {
               mainAxisSize: MainAxisSize.min,
               children: [
                 if (met != null && total != null && total > 0)
-                  StatusChip('${met.toInt()}/${total.toInt()} met',
+                  StatusChip('${met.toInt()}/${total.toInt()} erreicht',
                       tone: ChipTone.neutral),
                 InfoDot(
                   title: title,
                   body: info ??
-                      'Your $label, averaged across the last $_period. Tap a '
-                          'bar to drill into a finer period.',
+                      'Dein Wert für $label, gemittelt über die letzten '
+                          '$_period. Tippe auf einen Balken für einen feineren Zeitraum.',
                   methodNote: metric == 'spo2'
-                      ? 'This series is the IMPORTED vendor oxygen index — a '
-                            'relative saturation number, not a dip rate and '
-                            'not an absolute SpO₂. On-device decoding writes '
-                            'nothing here. Bars show each period’s value; '
-                            'periods with no measurement stay empty.'
-                      : 'Bars show each period’s value; periods with no '
-                            'measurement stay empty — a gap is a gap, never '
-                            'a zero.',
+                      ? 'Diese Reihe ist der IMPORTIERTE Sauerstoff-Index des '
+                            'Herstellers — eine relative Sättigungszahl, keine '
+                            'Abfallrate und kein absolutes SpO₂. Die Verarbeitung '
+                            'auf dem Gerät schreibt hier nichts. Balken zeigen den '
+                            'Wert jedes Zeitraums; Zeiträume ohne Messung bleiben leer.'
+                      : 'Balken zeigen den Wert jedes Zeitraums; Zeiträume ohne '
+                            'Messung bleiben leer — eine Lücke ist eine Lücke, '
+                            'niemals eine Null.',
                 ),
               ],
             ),
@@ -438,7 +438,7 @@ class TrendBoard extends StatelessWidget {
                 child: BigStat(
                   value: avg == null ? null : _fmtAvg(avg),
                   unit: showUnit ? unit : null,
-                  caption: 'average',
+                  caption: 'Durchschnitt',
                   size: BigStatSize.xl,
                 ),
               ),
@@ -449,7 +449,7 @@ class TrendBoard extends StatelessWidget {
                   unit: deltaUnit,
                   goodIsUp: trendGoodIsUp(metric),
                   showVsNormal: false,
-                  vsLabel: 'vs prev',
+                  vsLabel: 'vs. vorher',
                 ),
               ],
             ],
@@ -459,7 +459,7 @@ class TrendBoard extends StatelessWidget {
             SizedBox(
               height: 120,
               child: Center(
-                child: Text('No data in this period',
+                child: Text('Keine Daten in diesem Zeitraum',
                     style: AppText.captionMuted),
               ),
             )
@@ -476,8 +476,8 @@ class TrendBoard extends StatelessWidget {
             Center(
               child: Text(
                 scale == 'week'
-                    ? 'Tap a day for the full breakdown'
-                    : 'Tap a bar to drill in',
+                    ? 'Tippe auf einen Tag für alle Details'
+                    : 'Tippe auf einen Balken für mehr Details',
                 style: AppText.captionMuted,
               ),
             ),
diff --git a/lib/ui/screens/screens.dart b/lib/ui/screens/screens.dart
index 61c208f..34d2e54 100644
--- a/lib/ui/screens/screens.dart
+++ b/lib/ui/screens/screens.dart
@@ -32,7 +32,7 @@ class SleepScreen extends StatelessWidget {
   const SleepScreen({super.key});
   @override
   Widget build(BuildContext context) => MetricScreen(icon: OsIcon.activity, 
-    title: 'Sleep',
+    title: 'Schlaf',
     metric: 'sleep',
     accent: DomainAccent.sleep,
     // minutes → hours on bars. A MEASURED zero prints '0.0'; an absent night
@@ -59,7 +59,7 @@ class HeartScreen extends StatelessWidget {
   const HeartScreen({super.key});
   @override
   Widget build(BuildContext context) => MetricScreen(icon: OsIcon.activity, 
-    title: 'Heart',
+    title: 'Herz',
     metric: 'resting_hr',
     accent: DomainAccent.heart,
     todayDetail: (ctx) => Column(
@@ -81,7 +81,7 @@ class OxygenScreen extends StatelessWidget {
   const OxygenScreen({super.key});
   @override
   Widget build(BuildContext context) => MetricScreen(icon: OsIcon.activity, 
-    title: 'Overnight oxygen',
+    title: 'Sauerstoff über Nacht',
     metric: 'spo2',
     accent: DomainAccent.oxygen,
     // Was `v == 0 ? '0' : …` — it PRINTED a literal "0" above the bar for a
@@ -101,7 +101,7 @@ class WearScreen extends StatelessWidget {
   const WearScreen({super.key});
   @override
   Widget build(BuildContext context) => MetricScreen(icon: OsIcon.activity, 
-    title: 'Wear time',
+    title: 'Tragezeit',
     metric: 'wear',
     accent: AppColors.coralDeep,
     // minutes → hours on bars; a measured zero is a real (bad) day, not a gap.
@@ -119,7 +119,7 @@ class BodyScreen extends StatelessWidget {
   const BodyScreen({super.key});
   @override
   Widget build(BuildContext context) => MetricScreen(icon: OsIcon.activity, 
-    title: 'Body',
+    title: 'Körper',
     metric: 'strain',
     accent: DomainAccent.strain,
     action: Builder(
@@ -146,7 +146,7 @@ class BodyScreen extends StatelessWidget {
               AppIcon(OsIcon.ai, size: 16, color: AppColors.accent),
               const SizedBox(width: 6),
               Text(
-                'AI Coach',
+                'KI-Coach',
                 style: AppText.label.copyWith(color: AppColors.accent),
               ),
             ],
@@ -181,7 +181,7 @@ class ActivityScreen extends StatelessWidget {
   const ActivityScreen({super.key});
   @override
   Widget build(BuildContext context) => MetricScreen(icon: OsIcon.activity, 
-    title: 'Steps',
+    title: 'Schritte',
     metric: 'steps',
     accent: DomainAccent.steps,
     valueFmt: (v) => v == 0 ? '' : v.toStringAsFixed(0),
@@ -252,7 +252,7 @@ class _ActivityDetailState extends State<_ActivityDetail> {
       final buckets = ((t?['buckets'] as List?) ?? const [])
           .whereType<Map>()
           .toList();
-      const wd = ['M', 'T', 'W', 'T', 'F', 'S', 'S'];
+      const wd = ['M', 'D', 'M', 'D', 'F', 'S', 'S'];
       if (!mounted) return;
       setState(() {
         _week = [
@@ -355,27 +355,28 @@ class StepsDayContent extends StatelessWidget {
             crossAxisAlignment: CrossAxisAlignment.start,
             children: [
               TileHeader(
-                'Steps',
+                'Schritte',
                 icon: OsIcon.steps,
                 trailing: Row(
                   mainAxisSize: MainAxisSize.min,
                   children: [
-                    Tag('measured', color: accent),
+                    Tag('gemessen', color: accent),
                     InfoDot(
-                      title: 'How steps are counted',
+                      title: 'So werden Schritte gezählt',
                       body:
-                          'Only by something that can actually see your gait: '
-                          'your phone\'s own pedometer, or the band\'s 100 Hz '
-                          'sensor while it streams live (a workout, or with the '
-                          'app open). We never add the two together — they are '
-                          'the same walk seen from your pocket and your wrist.\n\n'
-                          'The rest of the day the band samples once a second, '
-                          'which is too slow to resolve individual steps. Those '
-                          'hours are left uncounted rather than estimated, so a '
-                          'day with no real measurement shows no number at all.',
+                          'Nur durch etwas, das deinen Gang wirklich erkennen '
+                          'kann: den Schrittzähler deines Handys oder den '
+                          '100-Hz-Sensor des Bands, während er live streamt '
+                          '(im Workout oder bei geöffneter App). Wir addieren '
+                          'die beiden nie — es ist derselbe Weg, gesehen aus '
+                          'deiner Tasche und von deinem Handgelenk.\n\n'
+                          'Den Rest des Tages misst das Band einmal pro Sekunde — '
+                          'zu langsam, um einzelne Schritte zu erkennen. Diese '
+                          'Stunden bleiben ungezählt statt geschätzt, daher zeigt '
+                          'ein Tag ohne echte Messung gar keine Zahl.',
                       methodNote:
-                          'Turn on “Use phone step count” in Profile → Health '
-                          'for all-day steps.',
+                          'Aktiviere „Schrittzählung vom Handy“ unter Profil → '
+                          'Gesundheit für Schritte über den ganzen Tag.',
                     ),
                   ],
                 ),
@@ -390,7 +391,7 @@ class StepsDayContent extends StatelessWidget {
                       // NOT "no steps yet" — absent means nothing that can
                       // resolve gait measured this day, which is a different
                       // statement from "you took zero steps".
-                      caption: steps > 0 ? 'goal $g' : 'not measured',
+                      caption: steps > 0 ? 'Ziel $g' : 'nicht gemessen',
                       size: BigStatSize.xl,
                     ),
                   ),
@@ -403,7 +404,7 @@ class StepsDayContent extends StatelessWidget {
                     valueText: steps > 0
                         ? '${((steps / g) * 100).clamp(0, 999).round()}%'
                         : '—',
-                    label: 'of goal',
+                    label: 'vom Ziel',
                   ),
                 ],
               ),
@@ -420,7 +421,7 @@ class StepsDayContent extends StatelessWidget {
               crossAxisAlignment: CrossAxisAlignment.start,
               mainAxisSize: MainAxisSize.min,
               children: [
-                const TileHeader('This week'),
+                const TileHeader('Diese Woche'),
                 const SizedBox(height: Sp.x3),
                 RingWeek(
                   values: ringValues,
@@ -445,8 +446,8 @@ class StepsDayContent extends StatelessWidget {
               ListRow(
                 icon: OsIcon.bodyStrain,
                 iconColor: accent,
-                title: 'Daily step goal',
-                value: goal == null ? 'Set' : '$goal',
+                title: 'Tägliches Schrittziel',
+                value: goal == null ? 'Festlegen' : '$goal',
                 onTap: onSetGoal,
               ),
             ],
@@ -467,8 +468,8 @@ class SpotCheckEntryCard extends StatelessWidget {
       child: ListRow(
         icon: OsIcon.hrv,
         iconColor: DomainAccent.recovery,
-        title: 'Live HRV spot check',
-        subtitle: 'A quick 60-second reading, any time',
+        title: 'Live-HRV-Spot-Check',
+        subtitle: 'Eine schnelle 60-Sekunden-Messung, jederzeit',
         onTap: () => Navigator.of(
           context,
         ).push(MaterialPageRoute(builder: (_) => const SpotCheckScreen())),
@@ -495,8 +496,8 @@ class CycleEntryCard extends StatelessWidget {
         child: ListRow(
           icon: OsIcon.calendar,
           iconColor: DomainAccent.heart,
-          title: 'Cycle tracking',
-          subtitle: 'Phase, next period & fertile window',
+          title: 'Zyklus-Tracking',
+          subtitle: 'Phase, nächste Periode & fruchtbares Fenster',
           onTap: () => Navigator.of(
             context,
           ).push(MaterialPageRoute(builder: (_) => const CycleScreen())),
diff --git a/lib/ui/screens/trend_screen.dart b/lib/ui/screens/trend_screen.dart
index 7c66f3d..43f1376 100644
--- a/lib/ui/screens/trend_screen.dart
+++ b/lib/ui/screens/trend_screen.dart
@@ -102,9 +102,9 @@ class _TrendTodayCardState extends State<_TrendTodayCard> {
   String _prettyDate(String ymd) {
     final d = DateTime.tryParse(ymd);
     if (d == null) return ymd;
-    const wd = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'];
-    const mon = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
-    return '${wd[(d.weekday - 1) % 7]}, ${mon[d.month - 1]} ${d.day}';
+    const wd = ['Mo', 'Di', 'Mi', 'Do', 'Fr', 'Sa', 'So'];
+    const mon = ['Jan', 'Feb', 'Mär', 'Apr', 'Mai', 'Jun', 'Jul', 'Aug', 'Sep', 'Okt', 'Nov', 'Dez'];
+    return '${wd[(d.weekday - 1) % 7]}, ${d.day}. ${mon[d.month - 1]}';
   }
 
   @override
@@ -149,7 +149,7 @@ class _TrendTodayCardState extends State<_TrendTodayCard> {
     final shown = value == null ? '—' : (fmt != null ? fmt(value) : (value == value.roundToDouble() ? value.toStringAsFixed(0) : value.toStringAsFixed(1)));
     final delta = summary?['delta_vs_prev'];
     final info = infoFor(widget.metric);
-    final header = isDay ? _prettyDate(widget.date!).toUpperCase() : 'LATEST';
+    final header = isDay ? _prettyDate(widget.date!).toUpperCase() : 'AKTUELL';
 
     return GlowCard(
       padding: const EdgeInsets.all(Sp.x6),
@@ -162,7 +162,7 @@ class _TrendTodayCardState extends State<_TrendTodayCard> {
         ]),
         const SizedBox(height: Sp.x3),
         if (isDay && value == null)
-          Text('No data for this day', style: AppText.title.copyWith(color: AppColors.inkSoft))
+          Text('Keine Daten für diesen Tag', style: AppText.title.copyWith(color: AppColors.inkSoft))
         else
           Row(crossAxisAlignment: CrossAxisAlignment.end, children: [
             Text(shown, style: AppText.display),
@@ -185,7 +185,7 @@ class _TrendTodayCardState extends State<_TrendTodayCard> {
                       : unit,
                   goodIsUp: trendGoodIsUp(widget.metric),
                   showVsNormal: false,
-                  vsLabel: 'vs prev',
+                  vsLabel: 'vs. vorher',
                 ),
               ),
             ],
@@ -196,7 +196,7 @@ class _TrendTodayCardState extends State<_TrendTodayCard> {
         ],
         if (!isDay) ...[
           const SizedBox(height: Sp.x2),
-          Text('Switch to Week · Month · 3M for the full trend.', style: AppText.captionMuted),
+          Text('Wechsle zu Woche · Monat · 3M für den ganzen Trend.', style: AppText.captionMuted),
         ],
       ]),
     );
