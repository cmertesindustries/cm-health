diff --git a/lib/ui/profile/about_screen.dart b/lib/ui/profile/about_screen.dart
index 8bbab5d..f23c481 100644
--- a/lib/ui/profile/about_screen.dart
+++ b/lib/ui/profile/about_screen.dart
@@ -54,7 +54,7 @@ class _AboutScreenState extends State<AboutScreen> {
     final ok = await _openUrl(url);
     if (!ok && mounted) {
       ScaffoldMessenger.of(context).showSnackBar(
-        const SnackBar(content: Text("Couldn't open the link.")),
+        const SnackBar(content: Text('Der Link konnte nicht geöffnet werden.')),
       );
     }
   }
@@ -66,7 +66,7 @@ class _AboutScreenState extends State<AboutScreen> {
         : '${_pkg!.version}+${_pkg!.buildNumber}';
 
     return AppScaffold(
-      title: 'About',
+      title: 'Über',
       children: [
         const SizedBox(height: Sp.x4),
         SurfaceCard(
@@ -74,27 +74,27 @@ class _AboutScreenState extends State<AboutScreen> {
           child: const AffiliationDisclaimer(),
         ),
         const SizedBox(height: Sp.x6),
-        const SectionHeader('Legal'),
+        const SectionHeader('Rechtliches'),
         _SettingsCard(rows: [
           ListRow(
             icon: OsIcon.activity,
-            title: 'Privacy policy',
+            title: 'Datenschutzerklärung',
             divider: true,
             onTap: () => _openUrlWithFeedback(kPrivacyPolicyUrl),
           ),
           ListRow(
             icon: OsIcon.activity,
-            title: 'Notice & attribution',
-            subtitle: 'Trademark + reverse-engineering disclosure',
+            title: 'Hinweise & Attribution',
+            subtitle: 'Marken- und Reverse-Engineering-Hinweis',
             divider: true,
             onTap: () => _openUrlWithFeedback(kNoticeUrl),
           ),
           ListRow(
             icon: OsIcon.activity,
-            title: 'Open-source licenses',
+            title: 'Open-Source-Lizenzen',
             onTap: () => showLicensePage(
               context: context,
-              applicationName: 'Edge',
+              applicationName: 'CM Health',
               applicationVersion: versionLabel,
             ),
           ),
@@ -104,7 +104,7 @@ class _AboutScreenState extends State<AboutScreen> {
         _SettingsCard(rows: [
           ListRow(
             icon: OsIcon.activity,
-            title: 'Edge',
+            title: 'CM Health',
             value: versionLabel ?? '…',
           ),
         ]),
diff --git a/lib/ui/profile/advanced_data_screen.dart b/lib/ui/profile/advanced_data_screen.dart
index a7af82e..2ea222c 100644
--- a/lib/ui/profile/advanced_data_screen.dart
+++ b/lib/ui/profile/advanced_data_screen.dart
@@ -97,8 +97,8 @@ class _AdvancedDataScreenState extends State<AdvancedDataScreen> {
         SnackBar(
           content: Text(
             n > 0
-                ? 'Recomputed $n selected day${n == 1 ? '' : 's'}.'
-                : 'No selected days were recomputed.',
+                ? '$n ausgewählte${n == 1 ? 'r' : ''} Tag${n == 1 ? '' : 'e'} neu berechnet.'
+                : 'Keine ausgewählten Tage wurden neu berechnet.',
           ),
         ),
       );
@@ -119,8 +119,8 @@ class _AdvancedDataScreenState extends State<AdvancedDataScreen> {
         SnackBar(
           content: Text(
             n > 0
-                ? 'Recomputed $n day${n == 1 ? '' : 's'}.'
-                : 'No raw data to analyze yet.',
+                ? '$n Tag${n == 1 ? '' : 'e'} neu berechnet.'
+                : 'Noch keine Rohdaten zum Analysieren.',
           ),
         ),
       );
@@ -161,10 +161,10 @@ class _AdvancedDataScreenState extends State<AdvancedDataScreen> {
                   child: Column(
                     crossAxisAlignment: CrossAxisAlignment.start,
                     children: [
-                      Text('Advanced data', style: AppText.h1),
+                      Text('Erweiterte Daten', style: AppText.h1),
                       const SizedBox(height: 2),
                       Text(
-                        'Developer tools for compute and sync',
+                        'Entwickler-Tools für Berechnung und Synchronisierung',
                         style: AppText.caption,
                       ),
                     ],
@@ -180,7 +180,7 @@ class _AdvancedDataScreenState extends State<AdvancedDataScreen> {
             ),
             const SizedBox(height: Sp.x6),
             SectionHeader(
-              'Compute',
+              'Berechnung',
               trailing: app.reanalyzing ? app.reanalyzeProgress : null,
             ),
             ProCard(
@@ -188,8 +188,8 @@ class _AdvancedDataScreenState extends State<AdvancedDataScreen> {
                 children: [
                   DetailRow(
                     icon: OsIcon.history,
-                    label: 'Recompute selected days',
-                    value: _selected.isEmpty ? 'Select first' : 'Run',
+                    label: 'Ausgewählte Tage neu berechnen',
+                    value: _selected.isEmpty ? 'Erst auswählen' : 'Starten',
                     onTap: _busy || _selected.isEmpty
                         ? null
                         : _reanalyzeSelected,
@@ -197,8 +197,8 @@ class _AdvancedDataScreenState extends State<AdvancedDataScreen> {
                   const Divider(height: 1),
                   DetailRow(
                     icon: OsIcon.history,
-                    label: 'Recompute all days',
-                    value: _busy ? 'Working…' : 'Run',
+                    label: 'Alle Tage neu berechnen',
+                    value: _busy ? 'Läuft…' : 'Starten',
                     onTap: _busy ? null : _reanalyzeAll,
                   ),
                 ],
@@ -211,52 +211,52 @@ class _AdvancedDataScreenState extends State<AdvancedDataScreen> {
                 crossAxisAlignment: CrossAxisAlignment.start,
                 children: [
                   _kv(
-                    'Latest data day',
+                    'Letzter Datentag',
                     _capture?['latest_raw_day']?.toString() ?? '—',
                   ),
                   _kv(
-                    'Latest data rec_ts',
+                    'Letzter Daten-rec_ts',
                     (_capture?['latest_raw_rec_ts'] ?? '—').toString(),
                   ),
                   _kv(
-                    'Decoded 1 Hz rows',
+                    'Dekodierte 1-Hz-Zeilen',
                     (_capture?['decoded_onehz'] ?? '—').toString(),
                   ),
                   _kv(
-                    'Decoded RR rows',
+                    'Dekodierte RR-Zeilen',
                     (_capture?['decoded_rr'] ?? '—').toString(),
                   ),
                   const SizedBox(height: Sp.x3),
                   _kv(
-                    'Today activity',
+                    'Aktivität heute',
                     _today?['activity_state']?.toString() ?? '—',
                   ),
                   _kv(
-                    'Today overnight',
+                    'Nacht heute',
                     _today?['overnight_state']?.toString() ?? '—',
                   ),
                   _kv(
-                    'Activity computed',
+                    'Aktivität berechnet',
                     _fmtMs((_today?['activity_computed_at'] as num?)?.toInt()),
                   ),
                   _kv(
-                    'Overnight computed',
+                    'Nacht berechnet',
                     _fmtMs((_today?['overnight_computed_at'] as num?)?.toInt()),
                   ),
                   const SizedBox(height: Sp.x3),
                   _kv(
-                    'Cross-day baseline',
-                    _crossday?['present'] == true ? 'Present' : 'Missing',
+                    'Tagesübergreifende Basislinie',
+                    _crossday?['present'] == true ? 'Vorhanden' : 'Fehlt',
                   ),
                   _kv(
-                    'Cross-day updated',
+                    'Tagesübergreifend aktualisiert',
                     _fmtMs((_crossday?['updated_at'] as num?)?.toInt()),
                   ),
                 ],
               ),
             ),
             const SizedBox(height: Sp.x6),
-            const SectionHeader('Storage'),
+            const SectionHeader('Speicher'),
             ProCard(
               child: Column(
                 crossAxisAlignment: CrossAxisAlignment.start,
@@ -274,10 +274,12 @@ class _AdvancedDataScreenState extends State<AdvancedDataScreen> {
             ),
             const SizedBox(height: Sp.x6),
             SectionHeader(
-              'Days',
+              'Tage',
               trailing: _days.isEmpty
                   ? null
-                  : (_selected.length == _days.length ? 'Clear' : 'Select all'),
+                  : (_selected.length == _days.length
+                      ? 'Auswahl aufheben'
+                      : 'Alle auswählen'),
               onTrailing: _days.isEmpty
                   ? null
                   : () {
@@ -332,7 +334,7 @@ class _AdvancedDataScreenState extends State<AdvancedDataScreen> {
           child: Text(row['table']?.toString() ?? '—', style: AppText.body),
         ),
         const SizedBox(width: Sp.x3),
-        Text('${_fmtRows(row['rows'])} rows', style: AppText.captionMuted),
+        Text('${_fmtRows(row['rows'])} Zeilen', style: AppText.captionMuted),
         const SizedBox(width: Sp.x3),
         Text(
           _fmtMb((row['mb'] as num?)),
@@ -379,8 +381,8 @@ class _AdvancedDataScreenState extends State<AdvancedDataScreen> {
                     Expanded(child: Text(dayId, style: AppText.title)),
                     Tag(
                       hasDerived
-                          ? (finalized ? 'finalized' : 'derived')
-                          : (rawCount > 0 ? 'raw-only' : 'empty'),
+                          ? (finalized ? 'finalisiert' : 'abgeleitet')
+                          : (rawCount > 0 ? 'nur Rohdaten' : 'leer'),
                       color: hasDerived
                           ? (finalized
                                 ? AppColors.goodSoft
@@ -394,9 +396,9 @@ class _AdvancedDataScreenState extends State<AdvancedDataScreen> {
                   spacing: Sp.x3,
                   runSpacing: Sp.x2,
                   children: [
-                    Text('Raw $rawCount', style: AppText.captionMuted),
+                    Text('Rohdaten $rawCount', style: AppText.captionMuted),
                     Text(
-                      'Derived ${hasDerived ? 'yes' : 'no'}',
+                      'Abgeleitet ${hasDerived ? 'ja' : 'nein'}',
                       style: AppText.captionMuted,
                     ),
                     Text(
@@ -404,7 +406,7 @@ class _AdvancedDataScreenState extends State<AdvancedDataScreen> {
                       style: AppText.captionMuted,
                     ),
                     Text(
-                      'Metrics ${(row['metric_count'] as int?) ?? 0}',
+                      'Metriken ${(row['metric_count'] as int?) ?? 0}',
                       style: AppText.captionMuted,
                     ),
                   ],
diff --git a/lib/ui/profile/data_history_screen.dart b/lib/ui/profile/data_history_screen.dart
index d0c0b54..0fa0a9a 100644
--- a/lib/ui/profile/data_history_screen.dart
+++ b/lib/ui/profile/data_history_screen.dart
@@ -75,14 +75,14 @@ class _DataHistoryScreenState extends State<DataHistoryScreen> {
       if (!mounted) return;
       await Share.shareXFiles(
         [XFile(path)],
-        text: 'OpenStrap data export',
+        text: 'CM Health Datenexport',
         sharePositionOrigin: origin,
       );
     } catch (e) {
       if (!mounted) return;
       ScaffoldMessenger.of(
         context,
-      ).showSnackBar(SnackBar(content: Text('Export failed: $e')));
+      ).showSnackBar(SnackBar(content: Text('Export fehlgeschlagen: $e')));
     } finally {
       if (mounted) setState(() => _busy = false);
     }
@@ -99,14 +99,14 @@ class _DataHistoryScreenState extends State<DataHistoryScreen> {
       await Share.shareXFiles(
         [XFile(path)],
         text:
-            'OpenStrap selected day export (${_selected.length} day${_selected.length == 1 ? '' : 's'})',
+            'CM Health Export ausgewählter Tage (${_selected.length} Tag${_selected.length == 1 ? '' : 'e'})',
         sharePositionOrigin: origin,
       );
     } catch (e) {
       if (!mounted) return;
       ScaffoldMessenger.of(
         context,
-      ).showSnackBar(SnackBar(content: Text('Export failed: $e')));
+      ).showSnackBar(SnackBar(content: Text('Export fehlgeschlagen: $e')));
     } finally {
       if (mounted) setState(() => _busy = false);
     }
@@ -119,18 +119,18 @@ class _DataHistoryScreenState extends State<DataHistoryScreen> {
     final confirm = await showDialog<bool>(
       context: context,
       builder: (context) => AlertDialog(
-        title: const Text('Delete selected days?'),
+        title: const Text('Ausgewählte Tage löschen?'),
         content: Text(
-          'This removes local raw and derived data for $count selected day${count == 1 ? '' : 's'}. Export first if you may need it later.',
+          'Das entfernt lokale Roh- und abgeleitete Daten für $count ausgewählte${count == 1 ? 'n' : ''} Tag${count == 1 ? '' : 'e'}. Exportiere sie vorher, falls du sie später noch brauchst.',
         ),
         actions: [
           TextButton(
             onPressed: () => Navigator.of(context).pop(false),
-            child: const Text('Cancel'),
+            child: const Text('Abbrechen'),
           ),
           FilledButton(
             onPressed: () => Navigator.of(context).pop(true),
-            child: const Text('Delete'),
+            child: const Text('Löschen'),
           ),
         ],
       ),
@@ -146,7 +146,7 @@ class _DataHistoryScreenState extends State<DataHistoryScreen> {
       ScaffoldMessenger.of(context).showSnackBar(
         SnackBar(
           content: Text(
-            'Deleted $deleted local row${deleted == 1 ? '' : 's'} across selected days.',
+            '$deleted lokale Zeile${deleted == 1 ? '' : 'n'} in den ausgewählten Tagen gelöscht.',
           ),
         ),
       );
@@ -185,9 +185,10 @@ class _DataHistoryScreenState extends State<DataHistoryScreen> {
                   child: Column(
                     crossAxisAlignment: CrossAxisAlignment.start,
                     children: [
-                      Text('Data history', style: AppText.h1),
+                      Text('Datenverlauf', style: AppText.h1),
                       const SizedBox(height: 2),
-                      Text('Manage your local data', style: AppText.caption),
+                      Text('Verwalte deine lokalen Daten',
+                          style: AppText.caption),
                     ],
                   ),
                 ),
@@ -206,40 +207,41 @@ class _DataHistoryScreenState extends State<DataHistoryScreen> {
             ProCard(
               child: Row(
                 children: [
-                  Expanded(child: _stat('Local size', _fmtBytes(_dbBytes))),
+                  Expanded(child: _stat('Lokale Größe', _fmtBytes(_dbBytes))),
                   const SizedBox(width: Sp.x3),
-                  Expanded(child: _stat('Days with data', '$rawDays')),
+                  Expanded(child: _stat('Tage mit Daten', '$rawDays')),
                   const SizedBox(width: Sp.x3),
-                  Expanded(child: _stat('Days derived', '$derivedDays')),
+                  Expanded(child: _stat('Abgeleitete Tage', '$derivedDays')),
                 ],
               ),
             ),
             const SizedBox(height: Sp.x6),
             SectionHeader(
-              'Actions',
-              trailing: selected > 0 ? '$selected selected' : 'Select days',
+              'Aktionen',
+              trailing: selected > 0 ? '$selected ausgewählt' : 'Tage auswählen',
             ),
             ProCard(
               child: Column(
                 children: [
                   DetailRow(
                     icon: OsIcon.sync,
-                    label: 'Export full database',
-                    value: 'Share .db',
+                    label: 'Gesamte Datenbank exportieren',
+                    value: '.db teilen',
                     onTap: _busy ? null : _shareWholeDb,
                   ),
                   const Divider(height: 1),
                   DetailRow(
                     icon: OsIcon.history,
-                    label: 'Export selected days',
-                    value: selected == 0 ? 'Select first' : 'Share .db',
+                    label: 'Ausgewählte Tage exportieren',
+                    value: selected == 0 ? 'Erst auswählen' : '.db teilen',
                     onTap: _busy || selected == 0 ? null : _shareSelected,
                   ),
                   const Divider(height: 1),
                   DetailRow(
                     icon: OsIcon.trash,
-                    label: 'Delete selected days',
-                    value: selected == 0 ? 'Select first' : 'Remove local data',
+                    label: 'Ausgewählte Tage löschen',
+                    value:
+                        selected == 0 ? 'Erst auswählen' : 'Lokale Daten entfernen',
                     onTap: _busy || selected == 0 ? null : _deleteSelected,
                   ),
                 ],
@@ -247,10 +249,12 @@ class _DataHistoryScreenState extends State<DataHistoryScreen> {
             ),
             const SizedBox(height: Sp.x6),
             SectionHeader(
-              'Days',
+              'Tage',
               trailing: _days.isEmpty
                   ? null
-                  : (_selected.length == _days.length ? 'Clear' : 'Select all'),
+                  : (_selected.length == _days.length
+                      ? 'Auswahl aufheben'
+                      : 'Alle auswählen'),
               onTrailing: _days.isEmpty
                   ? null
                   : () {
@@ -273,7 +277,7 @@ class _DataHistoryScreenState extends State<DataHistoryScreen> {
             else if (_days.isEmpty)
               ProCard(
                 child: Text(
-                  'No local day history yet.',
+                  'Noch kein lokaler Tagesverlauf.',
                   style: AppText.bodySoft,
                 ),
               )
@@ -347,13 +351,13 @@ class _DataHistoryScreenState extends State<DataHistoryScreen> {
                     Expanded(child: Text(dayId, style: AppText.title)),
                     if (hasDerived)
                       _pill(
-                        finalized ? 'Finalized' : 'Derived',
+                        finalized ? 'Finalisiert' : 'Abgeleitet',
                         finalized ? AppColors.goodSoft : AppColors.coralSoft,
                         finalized ? AppColors.good : AppColors.coralDeep,
                       )
                     else
                       _pill(
-                        rawCount > 0 ? 'Raw only' : 'Empty',
+                        rawCount > 0 ? 'Nur Rohdaten' : 'Leer',
                         AppColors.surfaceAlt,
                         AppColors.inkMuted,
                       ),
@@ -364,13 +368,13 @@ class _DataHistoryScreenState extends State<DataHistoryScreen> {
                   spacing: Sp.x3,
                   runSpacing: Sp.x2,
                   children: [
-                    Text('Raw $rawCount', style: AppText.captionMuted),
+                    Text('Rohdaten $rawCount', style: AppText.captionMuted),
                     Text(
-                      'Span ${_fmtTs((row['min_rec_ts'] as num?)?.toInt())}–${_fmtTs((row['max_rec_ts'] as num?)?.toInt())}',
+                      'Zeitraum ${_fmtTs((row['min_rec_ts'] as num?)?.toInt())}–${_fmtTs((row['max_rec_ts'] as num?)?.toInt())}',
                       style: AppText.captionMuted,
                     ),
                     Text(
-                      'Metrics ${(row['metric_count'] as int?) ?? 0}',
+                      'Metriken ${(row['metric_count'] as int?) ?? 0}',
                       style: AppText.captionMuted,
                     ),
                     Text(
@@ -378,7 +382,7 @@ class _DataHistoryScreenState extends State<DataHistoryScreen> {
                       style: AppText.captionMuted,
                     ),
                     Text(
-                      'Computed ${_fmtComputed((row['computed_at'] as num?)?.toInt())}',
+                      'Berechnet ${_fmtComputed((row['computed_at'] as num?)?.toInt())}',
                       style: AppText.captionMuted,
                     ),
                   ],
diff --git a/lib/ui/profile/gesture_section.dart b/lib/ui/profile/gesture_section.dart
index 0dadcd0..7f2d014 100644
--- a/lib/ui/profile/gesture_section.dart
+++ b/lib/ui/profile/gesture_section.dart
@@ -27,7 +27,7 @@ class GestureSettingsCard extends StatelessWidget {
           child: ListRow(
             icon: OsIcon.wear,
             iconColor: AppColors.accent,
-            title: 'Double-tap your band',
+            title: 'Doppeltippen auf dein Band',
             subtitle: settings.doubleTap.label,
             trailing:
                 AppIcon(OsIcon.arrowRight, size: 16, color: AppColors.onSurfaceFaint),
@@ -54,9 +54,9 @@ class GestureSettingsCard extends StatelessWidget {
               mainAxisSize: MainAxisSize.min,
               crossAxisAlignment: CrossAxisAlignment.start,
               children: [
-                Text('Double-tap action', style: AppText.h2),
+                Text('Doppeltipp-Aktion', style: AppText.h2),
                 const SizedBox(height: 4),
-                Text('What happens when you double-tap the band.',
+                Text('Was passiert, wenn du doppelt auf das Band tippst.',
                     style: AppText.captionMuted),
                 const SizedBox(height: Sp.x4),
                 Flexible(
@@ -80,7 +80,7 @@ class GestureSettingsCard extends StatelessWidget {
                         }),
                         if (options.length <= 1) ...[
                           const SizedBox(height: Sp.x3),
-                          Text('No band actions are available on this device yet.',
+                          Text('Auf diesem Gerät sind noch keine Band-Aktionen verfügbar.',
                               style: AppText.caption),
                         ],
                       ],
diff --git a/lib/ui/profile/notification_relay_section.dart b/lib/ui/profile/notification_relay_section.dart
index 37bc207..2c8b60b 100644
--- a/lib/ui/profile/notification_relay_section.dart
+++ b/lib/ui/profile/notification_relay_section.dart
@@ -28,12 +28,12 @@ class NotificationRelaySection extends StatelessWidget {
       animation: relay,
       builder: (context, _) {
         final subtitle = !relay.enabled
-            ? 'Off'
+            ? 'Aus'
             : !relay.permissionGranted
-                ? 'Needs notification access'
+                ? 'Benachrichtigungszugriff nötig'
                 : relay.appCount == 0
-                    ? 'On · no apps selected'
-                    : 'On · ${relay.appCount} app${relay.appCount == 1 ? '' : 's'}';
+                    ? 'An · keine Apps ausgewählt'
+                    : 'An · ${relay.appCount} App${relay.appCount == 1 ? '' : 's'}';
         return SurfaceCard(
           padding:
               const EdgeInsets.symmetric(horizontal: Sp.x4, vertical: Sp.x2),
@@ -43,7 +43,7 @@ class NotificationRelaySection extends StatelessWidget {
           child: ListRow(
             icon: OsIcon.notifications,
             iconColor: AppColors.accent,
-            title: 'Buzz on app notifications',
+            title: 'Vibration bei App-Benachrichtigungen',
             subtitle: subtitle,
             trailing: AppIcon(OsIcon.arrowRight,
                 size: 16, color: AppColors.onSurfaceFaint),
@@ -92,8 +92,8 @@ class _NotificationRelayScreenState extends State<NotificationRelayScreen>
   Widget build(BuildContext context) {
     final relay = context.read<AppState>().notificationRelay;
     return AppScaffold(
-      title: 'Band notifications',
-      subtitle: 'Buzz the strap when an app notifies',
+      title: 'Band-Benachrichtigungen',
+      subtitle: 'Vibration am Band, wenn eine App benachrichtigt',
       body: AnimatedBuilder(
         animation: relay,
         builder: (context, _) {
@@ -158,7 +158,9 @@ class _MasterToggle extends StatelessWidget {
             child: const OsAppIcon(OsIcon.notifications, size: 30),
           ),
           const SizedBox(width: Sp.x3),
-          Expanded(child: Text('Relay notifications', style: AppText.title)),
+          Expanded(
+              child:
+                  Text('Benachrichtigungen weiterleiten', style: AppText.title)),
           Switch.adaptive(
             value: relay.enabled,
             activeTrackColor: AppColors.accent,
@@ -179,11 +181,11 @@ class _GrantCard extends StatelessWidget {
       child: Column(
         crossAxisAlignment: CrossAxisAlignment.start,
         children: [
-          Text('Notification access needed', style: AppText.title),
+          Text('Benachrichtigungszugriff nötig', style: AppText.title),
           const SizedBox(height: 4),
           Text(
-            'Android needs your permission to read which app posted a notification. '
-            'We only use it to decide whether to buzz the band — nothing leaves your phone.',
+            'Android braucht deine Erlaubnis, um zu lesen, welche App eine Benachrichtigung gesendet hat. '
+            'Wir nutzen das nur, um zu entscheiden, ob das Band vibriert — nichts verlässt dein Telefon.',
             style: AppText.captionMuted,
           ),
           const SizedBox(height: Sp.x4),
@@ -195,7 +197,7 @@ class _GrantCard extends StatelessWidget {
                 visualDensity: VisualDensity.compact,
                 minimumSize: const Size(0, 44),
               ),
-              child: const Text('Grant access'),
+              child: const Text('Zugriff erlauben'),
             ),
           ),
         ],
@@ -213,7 +215,7 @@ class _SearchField extends StatelessWidget {
       onChanged: onChanged,
       style: AppText.body,
       decoration: InputDecoration(
-        hintText: 'Search apps',
+        hintText: 'Apps suchen',
         hintStyle: AppText.bodySoft.copyWith(color: AppColors.inkMuted),
         filled: true,
         fillColor: AppColors.surfaceAlt,
@@ -254,7 +256,8 @@ class _AppList extends StatelessWidget {
             : all.where((a) => a.name.toLowerCase().contains(q)).toList())
           ..sort((a, b) => a.name.toLowerCase().compareTo(b.name.toLowerCase()));
         if (apps.isEmpty) {
-          return Center(child: Text('No apps found', style: AppText.captionMuted));
+          return Center(
+              child: Text('Keine Apps gefunden', style: AppText.captionMuted));
         }
         return ListView.separated(
           physics: const BouncingScrollPhysics(),
diff --git a/lib/ui/profile/notification_settings_screen.dart b/lib/ui/profile/notification_settings_screen.dart
index c2b4e3e..3aa6dde 100644
--- a/lib/ui/profile/notification_settings_screen.dart
+++ b/lib/ui/profile/notification_settings_screen.dart
@@ -71,7 +71,7 @@ class _NotificationSettingsScreenState
     if (min < 60) return '$min min';
     final h = min / 60;
     final label = h == h.roundToDouble() ? '${h.toInt()}' : h.toString();
-    return '$label hr${h == 1 ? '' : 's'}';
+    return '$label Std.';
   }
 
   Future<void> _pickTime(bool start) async {
@@ -90,17 +90,18 @@ class _NotificationSettingsScreenState
   @override
   Widget build(BuildContext context) {
     return AppScaffold(
-      title: 'Notifications',
-      subtitle: 'Alerts, recovery & reminders',
+      title: 'Benachrichtigungen',
+      subtitle: 'Warnungen, Erholung & Erinnerungen',
       actions: const [
         InfoDot(
-          title: 'How notifications work',
+          title: 'So funktionieren Benachrichtigungen',
           bullets: [
-            'Everything is generated on this device from your own data — '
-                'nothing is sent to a server.',
-            'Your in-app history keeps every alert even when a category '
-                'is off.',
-            'Health alerts can break through quiet hours if you allow it.',
+            'Alles wird auf diesem Gerät aus deinen eigenen Daten erzeugt — '
+                'nichts wird an einen Server gesendet.',
+            'Dein In-App-Verlauf behält jede Warnung, auch wenn eine '
+                'Kategorie aus ist.',
+            'Gesundheitswarnungen können die Ruhezeiten durchbrechen, wenn '
+                'du es erlaubst.',
           ],
         ),
       ],
@@ -108,32 +109,32 @@ class _NotificationSettingsScreenState
         if (!_loaded)
           Skeleton.tileRow(rows: 3)
         else ...[
-          const SectionHeader('What you get'),
+          const SectionHeader('Was du bekommst'),
           SurfaceCard(
             child: Column(children: [
               _toggle(
-                title: 'Health alerts',
+                title: 'Gesundheitswarnungen',
                 subtitle:
-                    'Possible illness, unusual overnight physiology and '
-                    'elevated temperature. High priority.',
+                    'Mögliche Krankheit, ungewöhnliche nächtliche Physiologie '
+                    'und erhöhte Temperatur. Hohe Priorität.',
                 value: _p.healthEnabled,
                 onChanged: (v) => _update(_p.copyWith(healthEnabled: v)),
               ),
               const _HairLine(),
               _toggle(
-                title: 'Recovery',
+                title: 'Erholung',
                 subtitle:
-                    'Your daily recovery readiness and notable shifts in '
-                    'your trends.',
+                    'Deine tägliche Erholung und auffällige Veränderungen '
+                    'in deinen Trends.',
                 value: _p.recoveryEnabled,
                 onChanged: (v) => _update(_p.copyWith(recoveryEnabled: v)),
               ),
               const _HairLine(),
               _toggle(
-                title: 'Reminders',
+                title: 'Erinnerungen',
                 subtitle:
-                    'Wind-down, movement nudges, step goal and the weekly '
-                    'recap.',
+                    'Abendliches Runterkommen, Bewegungsimpulse, Schrittziel '
+                    'und der wöchentliche Rückblick.',
                 value: _p.remindersEnabled,
                 onChanged: (v) =>
                     _update(_p.copyWith(remindersEnabled: v), reschedule: true),
@@ -141,14 +142,15 @@ class _NotificationSettingsScreenState
             ]),
           ),
           const SizedBox(height: Sp.x6),
-          const SectionHeader('Hydration'),
+          const SectionHeader('Trinken'),
           SurfaceCard(
             child: Column(children: [
               _toggle(
-                title: 'Water reminder',
+                title: 'Trink-Erinnerung',
                 subtitle:
-                    'A gentle nudge across your waking hours, and a buzz on '
-                    'your strap when it\'s connected. Silent in quiet hours.',
+                    'Ein sanfter Anstupser über deine Wachzeit hinweg und '
+                    'eine Vibration am Band, wenn es verbunden ist. Still '
+                    'während der Ruhezeiten.',
                 value: _p.waterEnabled,
                 onChanged: (v) =>
                     _update(_p.copyWith(waterEnabled: v), reschedule: true),
@@ -160,9 +162,9 @@ class _NotificationSettingsScreenState
                   child: Column(
                     crossAxisAlignment: CrossAxisAlignment.start,
                     children: [
-                      Text('How often', style: AppText.title),
+                      Text('Wie oft', style: AppText.title),
                       const SizedBox(height: 2),
-                      Text('Every ${_fmtInterval(_p.waterIntervalMin)}.',
+                      Text('Alle ${_fmtInterval(_p.waterIntervalMin)}.',
                           style: AppText.captionMuted),
                       const SizedBox(height: Sp.x3),
                       Wrap(
@@ -186,34 +188,35 @@ class _NotificationSettingsScreenState
             ]),
           ),
           const SizedBox(height: Sp.x6),
-          const SectionHeader('Quiet hours'),
+          const SectionHeader('Ruhezeiten'),
           SurfaceCard(
             child: Column(children: [
               _toggle(
-                title: 'Silence during quiet hours',
-                subtitle: 'Recovery and reminders stay silent in this window.',
+                title: 'Stumm während der Ruhezeiten',
+                subtitle:
+                    'Erholung und Erinnerungen bleiben in diesem Zeitraum stumm.',
                 value: _p.quietEnabled,
                 onChanged: (v) => _update(_p.copyWith(quietEnabled: v)),
               ),
               if (_p.quietEnabled) ...[
                 const _HairLine(),
                 ListRow(
-                  title: 'From',
+                  title: 'Von',
                   value: _fmt(_p.quietStartMin),
                   divider: true,
                   onTap: () => _pickTime(true),
                 ),
                 ListRow(
-                  title: 'To',
+                  title: 'Bis',
                   value: _fmt(_p.quietEndMin),
                   divider: true,
                   onTap: () => _pickTime(false),
                 ),
                 _toggle(
-                  title: 'Let health alerts through',
+                  title: 'Gesundheitswarnungen durchlassen',
                   subtitle:
-                      'Illness and temperature alerts can still notify you '
-                      'during quiet hours.',
+                      'Krankheits- und Temperaturwarnungen können dich auch '
+                      'während der Ruhezeiten benachrichtigen.',
                   value: _p.criticalOverridesQuiet,
                   onChanged: (v) =>
                       _update(_p.copyWith(criticalOverridesQuiet: v)),
diff --git a/lib/ui/profile/profile_screen.dart b/lib/ui/profile/profile_screen.dart
index b828f88..7936d60 100644
--- a/lib/ui/profile/profile_screen.dart
+++ b/lib/ui/profile/profile_screen.dart
@@ -13,6 +13,7 @@ import 'package:flutter/material.dart';
 import 'package:flutter/services.dart';
 import 'package:provider/provider.dart';
 import 'package:share_plus/share_plus.dart';
+import 'diagnose_export.dart';
 import 'package:url_launcher/url_launcher.dart';
 
 import '../../data/db.dart';
@@ -55,15 +56,16 @@ Future<void> _backupSheet(BuildContext ctx, AppState app) async {
           mainAxisSize: MainAxisSize.min,
           crossAxisAlignment: CrossAxisAlignment.start,
           children: [
-            Text('Automatic backup', style: AppText.h2),
+            Text('Automatisches Backup', style: AppText.h2),
             const SizedBox(height: Sp.x3),
             Text(
-              'A full copy of your database, kept in an OpenStrap Backups '
-              'folder you can reach from Files. Point iCloud Drive, Synology '
-              'or Nextcloud at it and your history lives somewhere other than '
-              'this phone. The last $kBackupsKept are kept.\n\n'
-              'It is not encrypted, and it runs when you open the app rather '
-              'than in the background.',
+              'Eine vollständige Kopie deiner Datenbank im Ordner „OpenStrap '
+              'Backups", den du über die Dateien-App erreichst. Verbinde '
+              'iCloud Drive, Synology oder Nextcloud damit, und dein Verlauf '
+              'liegt nicht nur auf diesem Telefon. Die letzten $kBackupsKept '
+              'werden behalten.\n\n'
+              'Das Backup ist nicht verschlüsselt und läuft beim Öffnen der '
+              'App, nicht im Hintergrund.',
               style: AppText.bodySoft.copyWith(color: AppColors.inkSoft),
             ),
             const SizedBox(height: Sp.x4),
@@ -92,8 +94,8 @@ Future<void> _backupSheet(BuildContext ctx, AppState app) async {
     SnackBar(
       content: Text(
         chosen == BackupCadence.off
-            ? 'Automatic backup off'
-            : 'Backing up ${chosen.label.toLowerCase()}',
+            ? 'Automatisches Backup aus'
+            : 'Backup: ${chosen.label.toLowerCase()}',
       ),
     ),
   );
@@ -112,7 +114,7 @@ Future<void> _importHealthProfile(BuildContext ctx, AppState app) async {
   if (!await importer.requestPermission()) {
     if (!ctx.mounted) return;
     messenger.showSnackBar(
-      SnackBar(content: Text('$store did not grant access')),
+      SnackBar(content: Text('$store hat den Zugriff nicht erlaubt')),
     );
     return;
   }
@@ -120,21 +122,21 @@ Future<void> _importHealthProfile(BuildContext ctx, AppState app) async {
   if (!ctx.mounted) return;
   if (snap.isEmpty) {
     messenger.showSnackBar(
-      SnackBar(content: Text('Nothing to read from $store')),
+      SnackBar(content: Text('Nichts aus $store zu lesen')),
     );
     return;
   }
   final changed = healthProfileChanges(app.user, snap);
   if (changed.isEmpty) {
     messenger.showSnackBar(
-      const SnackBar(content: Text('Your profile already matches')),
+      const SnackBar(content: Text('Dein Profil ist bereits aktuell')),
     );
     return;
   }
   await app.updateProfile(mergeHealthProfile(app.user, snap));
   if (!ctx.mounted) return;
   messenger.showSnackBar(
-    SnackBar(content: Text('Updated ${changed.join(', ')}')),
+    SnackBar(content: Text('${changed.join(', ')} aktualisiert')),
   );
 }
 
@@ -187,7 +189,7 @@ class ProfileScreen extends StatelessWidget {
 
     return AppScaffold(
       titleWidget: Text(
-        name.isEmpty ? 'Your profile' : name,
+        name.isEmpty ? 'Dein Profil' : name,
         style: AppText.h1,
         maxLines: 1,
         overflow: TextOverflow.ellipsis,
@@ -198,7 +200,7 @@ class ProfileScreen extends StatelessWidget {
       ],
       children: [
           // ── Your device ──────────────────────────────────────────────
-          const SectionHeader('Your device'),
+          const SectionHeader('Dein Gerät'),
           _deviceTile(context, app),
           // Android: battery-optimization (Doze) exemption — without it the OS
           // can freeze the background BLE session between events overnight.
@@ -215,67 +217,67 @@ class ProfileScreen extends StatelessWidget {
           const SizedBox(height: Sp.x6),
 
           // ── Profile ──────────────────────────────────────────────────
-          const SectionHeader('Profile'),
+          const SectionHeader('Profil'),
           _SettingsCard(rows: [
             ListRow(
               title: 'Name',
-              value: name.isEmpty ? 'Add' : name,
+              value: name.isEmpty ? 'Hinzufügen' : name,
               divider: true,
               onTap: () => _editProfileSheet(context, app),
             ),
             ListRow(
-              title: 'Sex',
+              title: 'Geschlecht',
               value: _sexLabel(user['sex']?.toString()),
               divider: true,
               onTap: () => _editProfileSheet(context, app),
             ),
             ListRow(
-              title: 'Age',
-              value: user['age'] != null ? '${user['age']}' : 'Add',
+              title: 'Alter',
+              value: user['age'] != null ? '${user['age']}' : 'Hinzufügen',
               divider: true,
               onTap: () => _editProfileSheet(context, app),
             ),
             ListRow(
-              title: 'Height',
+              title: 'Größe',
               value: user['height_cm'] != null
                   ? units.height(user['height_cm'] as num?)
-                  : 'Add',
+                  : 'Hinzufügen',
               divider: true,
               onTap: () => _editProfileSheet(context, app),
             ),
             ListRow(
-              title: 'Weight',
+              title: 'Gewicht',
               value: user['weight_kg'] != null
                   ? units.weight(user['weight_kg'] as num?)
-                  : 'Add',
+                  : 'Hinzufügen',
               divider: true,
               onTap: () => _editProfileSheet(context, app),
             ),
             Builder(
               builder: (rowCtx) => ListRow(
                 icon: OsIcon.sync,
-                title: 'Fill from ${app.healthStoreName}',
-                value: 'Import',
+                title: 'Aus ${app.healthStoreName} übernehmen',
+                value: 'Importieren',
                 onTap: () => _importHealthProfile(rowCtx, app),
               ),
             ),
           ]),
           const _CardNote(
-            'Body metrics improve your calorie estimate. Weight is the one '
-            'that drifts — importing keeps it current.',
+            'Körperdaten verbessern deine Kalorienschätzung. Das Gewicht '
+            'ändert sich am ehesten — Importieren hält es aktuell.',
           ),
 
           const SizedBox(height: Sp.x6),
 
           // ── Goals ────────────────────────────────────────────────────
-          const SectionHeader('Goals'),
+          const SectionHeader('Ziele'),
           _SettingsCard(rows: [
             ListRow(
               icon: OsIcon.steps,
-              title: 'Daily step goal',
+              title: 'Tägliches Schrittziel',
               value: user['step_goal'] != null
-                  ? '${user['step_goal']} steps'
-                  : 'Set',
+                  ? '${user['step_goal']} Schritte'
+                  : 'Festlegen',
               onTap: () => Navigator.of(context).push(
                 themedRoute(
                   (_) => StepGoalScreen(
@@ -290,12 +292,12 @@ class ProfileScreen extends StatelessWidget {
           const SizedBox(height: Sp.x6),
 
           // ── Data ─────────────────────────────────────────────────────
-          const SectionHeader('Data'),
+          const SectionHeader('Daten'),
           _SettingsCard(rows: [
             ListRow(
               icon: OsIcon.history,
-              title: 'Import data',
-              value: 'NOOP · Edge · WHOOP',
+              title: 'Daten importieren',
+              value: 'NOOP · CM Health · WHOOP',
               divider: true,
               onTap: () => Navigator.of(
                 context,
@@ -303,21 +305,21 @@ class ProfileScreen extends StatelessWidget {
             ),
             ListRow(
               icon: OsIcon.sync,
-              title: 'Companion URL',
+              title: 'Companion-URL',
               value: app.companionConfigured
                   ? _shortUrl(app.companionUrl)
-                  : 'Not set',
+                  : 'Nicht gesetzt',
               divider: true,
               onTap: () => _editCompanionUrl(context, app),
             ),
             ListRow(
               icon: OsIcon.activity,
-              title: 'Re-analyze data',
+              title: 'Daten neu analysieren',
               value: app.reanalyzing
                   ? (app.reanalyzeProgress.isEmpty
-                        ? 'Working…'
+                        ? 'Läuft…'
                         : app.reanalyzeProgress)
-                  : 'Run',
+                  : 'Starten',
               divider: true,
               onTap: () async {
                 if (app.reanalyzing) return;
@@ -327,8 +329,8 @@ class ProfileScreen extends StatelessWidget {
                   SnackBar(
                     content: Text(
                       n > 0
-                          ? 'Analyzed $n day${n == 1 ? '' : 's'} of stored data.'
-                          : 'No raw data to analyze yet.',
+                          ? '$n Tag${n == 1 ? '' : 'e'} gespeicherter Daten analysiert.'
+                          : 'Noch keine Rohdaten zum Analysieren.',
                     ),
                   ),
                 );
@@ -340,8 +342,8 @@ class ProfileScreen extends StatelessWidget {
             Builder(
               builder: (rowCtx) => ListRow(
                 icon: OsIcon.server,
-                title: 'Export data (.db)',
-                value: 'Share',
+                title: 'Daten exportieren (.db)',
+                value: 'Teilen',
                 onTap: () async {
                   final messenger = ScaffoldMessenger.of(rowCtx);
                   final origin = shareOriginFor(rowCtx);
@@ -352,22 +354,44 @@ class ProfileScreen extends StatelessWidget {
                     if (!rowCtx.mounted) return;
                     await Share.shareXFiles(
                       [XFile(path)],
-                      text: 'OpenStrap data export',
+                      text: 'CM Health Datenexport',
                       sharePositionOrigin: origin,
                     );
                   } catch (e) {
                     messenger.showSnackBar(
-                      SnackBar(content: Text('Export failed: $e')),
+                      SnackBar(content: Text('Export fehlgeschlagen: $e')),
                     );
                   }
                 },
                 divider: true,
               ),
             ),
+            // CM Health (v2.0.1): one-tap diagnose report — app state + logs
+            // straight into the share sheet, for remote debugging.
+            Builder(
+              builder: (rowCtx) => ListRow(
+                icon: OsIcon.info,
+                title: 'Diagnose exportieren',
+                value: 'Teilen',
+                divider: true,
+                onTap: () async {
+                  final messenger = ScaffoldMessenger.of(rowCtx);
+                  final origin = shareOriginFor(rowCtx);
+                  try {
+                    await shareDiagnoseReport(rowCtx, app,
+                        sharePositionOrigin: origin);
+                  } catch (e) {
+                    messenger.showSnackBar(
+                      SnackBar(content: Text('Diagnose fehlgeschlagen: $e')),
+                    );
+                  }
+                },
+              ),
+            ),
             Builder(
               builder: (rowCtx) => ListRow(
                 icon: OsIcon.history,
-                title: 'Automatic backup',
+                title: 'Automatisches Backup',
                 value: app.backupCadence.label,
                 divider: true,
                 onTap: () => _backupSheet(rowCtx, app),
@@ -377,8 +401,8 @@ class ProfileScreen extends StatelessWidget {
             // record you consult, not a number that changes overnight.
             ListRow(
               icon: OsIcon.ecgRhythm,
-              title: 'Labs',
-              value: 'Blood work',
+              title: 'Laborwerte',
+              value: 'Blutwerte',
               divider: true,
               onTap: () => Navigator.of(context).push(
                 themedRoute((_) => const LabsScreen(), name: 'LabsScreen'),
@@ -390,8 +414,8 @@ class ProfileScreen extends StatelessWidget {
             Builder(
               builder: (rowCtx) => ListRow(
                 icon: OsIcon.share,
-                title: 'Export data (.csv)',
-                value: 'Share',
+                title: 'Daten exportieren (.csv)',
+                value: 'Teilen',
                 onTap: () async {
                   // A second export while the first share sheet is still open
                   // would start cleaning up run directories underneath it. The
@@ -410,8 +434,8 @@ class ProfileScreen extends StatelessWidget {
                         SnackBar(
                           content: Text(
                             result.hasFailures
-                                ? 'Export failed'
-                                : 'Nothing to export yet',
+                                ? 'Export fehlgeschlagen'
+                                : 'Noch nichts zu exportieren',
                           ),
                         ),
                       );
@@ -421,20 +445,20 @@ class ProfileScreen extends StatelessWidget {
                       messenger.showSnackBar(
                         SnackBar(
                           content: Text(
-                            'Exported all but ${result.failed.join(', ')}',
+                            'Alles exportiert außer ${result.failed.join(', ')}',
                           ),
                         ),
                       );
                     }
                     await Share.shareXFiles(
                       [for (final p in result.paths) XFile(p)],
-                      text: 'OpenStrap CSV export',
+                      text: 'CM Health CSV-Export',
                       sharePositionOrigin: origin,
                     );
                   } catch (e) {
                     if (!rowCtx.mounted) return;
                     messenger.showSnackBar(
-                      SnackBar(content: Text('Export failed: $e')),
+                      SnackBar(content: Text('Export fehlgeschlagen: $e')),
                     );
                   } finally {
                     _csvExportInFlight = false;
@@ -446,8 +470,8 @@ class ProfileScreen extends StatelessWidget {
             // Per-day data manager (browse, export, delete stored days).
             ListRow(
               icon: OsIcon.history,
-              title: 'Data history',
-              value: 'Manage',
+              title: 'Datenverlauf',
+              value: 'Verwalten',
               divider: advancedDebugMode,
               onTap: () => Navigator.of(
                 context,
@@ -458,8 +482,8 @@ class ProfileScreen extends StatelessWidget {
             if (advancedDebugMode)
               ListRow(
                 icon: OsIcon.settings,
-                title: 'Advanced data',
-                value: 'Debug tools',
+                title: 'Erweiterte Daten',
+                value: 'Debug-Tools',
                 onTap: () => Navigator.of(
                   context,
                 ).push(themedRoute((_) => const AdvancedDataScreen(),
@@ -476,7 +500,7 @@ class ProfileScreen extends StatelessWidget {
           const SizedBox(height: Sp.x6),
 
           // ── Units (local display preference) ─────────────────────────
-          const SectionHeader('Units'),
+          const SectionHeader('Einheiten'),
           SurfaceCard(
             padding: const EdgeInsets.all(Sp.x4),
             child: Column(
@@ -504,14 +528,15 @@ class ProfileScreen extends StatelessWidget {
           const SizedBox(height: Sp.x6),
 
           // ── Cycle tracking (explicit opt-in) ─────────────────────────
-          const SectionHeader('Cycle tracking'),
+          const SectionHeader('Zyklus-Tracking'),
           _ToggleCard(
             icon: OsIcon.calendar,
             iconColor: DomainAccent.cycle,
-            title: 'Track menstrual cycle',
+            title: 'Menstruationszyklus verfolgen',
             subtitle:
-                'Log periods and see phase, next period & fertile window. '
-                'Off by default — nothing is computed until you turn this on.',
+                'Erfasse deine Periode und sieh Phase, nächste Periode & '
+                'fruchtbares Fenster. Standardmäßig aus — nichts wird '
+                'berechnet, bis du es einschaltest.',
             value: user['track_cycle'] == true || user['track_cycle'] == 1,
             onChanged: (v) async {
               try {
@@ -523,13 +548,13 @@ class ProfileScreen extends StatelessWidget {
           const SizedBox(height: Sp.x6),
 
           // ── Privacy (opt-in companion data — also offered at onboarding) ──
-          const SectionHeader('Privacy'),
+          const SectionHeader('Privatsphäre'),
           _ToggleCard(
             icon: OsIcon.info,
-            title: 'Send anonymous diagnostics',
+            title: 'Anonyme Diagnosedaten senden',
             subtitle:
-                'Crash/error reports plus basic device info. No health data. '
-                'On by default — switch off anytime.',
+                'Absturz-/Fehlerberichte plus grundlegende Geräteinfos. Keine '
+                'Gesundheitsdaten. Standardmäßig an — jederzeit abschaltbar.',
             value: app.telemetryConsent,
             onChanged: (v) => app.setTelemetryConsent(v),
           ),
@@ -542,11 +567,11 @@ class ProfileScreen extends StatelessWidget {
             const SizedBox(height: Sp.x3),
             _ToggleCard(
               icon: OsIcon.activity,
-              title: 'Contribute my health data',
+              title: 'Meine Gesundheitsdaten beitragen',
               subtitle:
-                  'Periodically upload your on-device database (over Wi-Fi, '
-                  'while charging) to improve the algorithms. On by default — '
-                  'switch off anytime.',
+                  'Lädt regelmäßig deine lokale Datenbank hoch (über WLAN, '
+                  'beim Laden), um die Algorithmen zu verbessern. '
+                  'Standardmäßig an — jederzeit abschaltbar.',
               value: app.healthShareConsent,
               onChanged: (v) => app.setHealthShareConsent(v),
             ),
@@ -555,13 +580,13 @@ class ProfileScreen extends StatelessWidget {
           const SizedBox(height: Sp.x6),
 
           // ── Appearance ───────────────────────────────────────────────
-          const SectionHeader('Appearance'),
+          const SectionHeader('Erscheinungsbild'),
           const SurfaceCard(child: AppearanceSelector(labeled: true)),
 
           const SizedBox(height: Sp.x6),
 
           // ── Gestures ─────────────────────────────────────────────────
-          const SectionHeader('Gestures'),
+          const SectionHeader('Gesten'),
           const GestureSettingsCard(),
 
           const SizedBox(height: Sp.x6),
@@ -570,7 +595,7 @@ class ProfileScreen extends StatelessWidget {
           // Android-only: TaskerReceiver + the broadcast_to_tasker capability
           // have no iOS counterpart (same convention as _KeepAliveRow above).
           if (Platform.isAndroid) ...[
-            const SectionHeader('Automation'),
+            const SectionHeader('Automatisierung'),
             _SettingsCard(rows: [
               GestureDetector(
                 onLongPress: () async {
@@ -578,12 +603,12 @@ class ProfileScreen extends StatelessWidget {
                     const ClipboardData(text: 'wtf.openstrap.openstrap_edge.BUZZ_STRAP'),
                   );
                   if (context.mounted) {
-                    _snack(context, 'Copied BUZZ_STRAP action.');
+                    _snack(context, 'BUZZ_STRAP-Action kopiert.');
                   }
                 },
                 child: ListRow(
                   icon: OsIcon.activity,
-                  title: 'Tasker buzz strap',
+                  title: 'Tasker: Band vibrieren',
                   subtitle: 'wtf.openstrap.openstrap_edge.BUZZ_STRAP',
                   divider: true,
                   onTap: null,
@@ -592,12 +617,13 @@ class ProfileScreen extends StatelessWidget {
               const _TaskerTokenRow(),
             ]),
             const _CardNote(
-              'Send a Broadcast intent from Tasker or any automation app to '
-              'vibrate your WHOOP strap. On Android 12+, set Package to '
-              'wtf.openstrap.openstrap_edge. Requires a String extra "token" '
-              'matching the automation token below (long-press to copy) — '
-              'without it, the strap won\'t buzz. Optional int extra '
-              '"pattern" (default 2).',
+              'Sende einen Broadcast-Intent aus Tasker oder einer anderen '
+              'Automatisierungs-App, um dein WHOOP-Band vibrieren zu lassen. '
+              'Auf Android 12+ setze Package auf wtf.openstrap.openstrap_edge. '
+              'Erfordert ein String-Extra "token", das dem '
+              'Automatisierungs-Token unten entspricht (zum Kopieren lange '
+              'drücken) — ohne das vibriert das Band nicht. Optionales '
+              'int-Extra "pattern" (Standard 2).',
             ),
             const SizedBox(height: Sp.x3),
             _SettingsCard(rows: [
@@ -607,31 +633,32 @@ class ProfileScreen extends StatelessWidget {
                     const ClipboardData(text: 'wtf.openstrap.openstrap_edge.DOUBLE_TAP'),
                   );
                   if (context.mounted) {
-                    _snack(context, 'Copied DOUBLE_TAP action.');
+                    _snack(context, 'DOUBLE_TAP-Action kopiert.');
                   }
                 },
                 child: ListRow(
                   icon: OsIcon.activity,
-                  title: 'Double-tap broadcast',
+                  title: 'Doppeltipp-Broadcast',
                   subtitle: 'wtf.openstrap.openstrap_edge.DOUBLE_TAP',
                   onTap: null,
                 ),
               ),
             ]),
             const _CardNote(
-              'Set double-tap gesture to "Broadcast to Tasker", then create '
-              'a Tasker profile: Event → Intent Received → action above.',
+              'Stelle die Doppeltipp-Geste auf "Broadcast to Tasker" und lege '
+              'dann ein Tasker-Profil an: Event → Intent Received → Action '
+              'oben.',
             ),
           ],
           const SizedBox(height: Sp.x6),
 
           // ── Notifications ────────────────────────────────────────────
-          const SectionHeader('Notifications'),
+          const SectionHeader('Benachrichtigungen'),
           _SettingsCard(rows: [
             ListRow(
               icon: OsIcon.notifications,
-              title: 'Alerts & reminders',
-              value: 'Manage',
+              title: 'Warnungen & Erinnerungen',
+              value: 'Verwalten',
               onTap: () => Navigator.of(
                 context,
               ).push(themedRoute((_) => const NotificationSettingsScreen(),
@@ -649,21 +676,21 @@ class ProfileScreen extends StatelessWidget {
           // Two distinct AI surfaces get their own row here — previously the
           // agentic AI Coach chat was reachable ONLY via a small pill button
           // on the Body screen, with nothing under Profile pointing at it.
-          const SectionHeader('AI'),
+          const SectionHeader('KI'),
           _SettingsCard(rows: [
             ListRow(
               icon: OsIcon.ai,
-              title: 'Briefings & journal',
-              subtitle: 'Your AI key, morning/evening briefings, journal prompt',
-              value: 'Manage',
+              title: 'Briefings & Journal',
+              subtitle: 'Dein KI-Schlüssel, Morgen-/Abend-Briefings, Journal-Prompt',
+              value: 'Verwalten',
               onTap: () => Navigator.of(context).push(themedRoute(
                   (_) => const AiSettingsScreen(), name: 'AiSettingsScreen')),
             ),
             ListRow(
               icon: OsIcon.ai,
-              title: 'AI coach',
-              subtitle: 'Chat with your on-device AI about your own data',
-              value: 'Open',
+              title: 'KI-Coach',
+              subtitle: 'Chatte mit deiner KI auf dem Gerät über deine eigenen Daten',
+              value: 'Öffnen',
               onTap: () => Navigator.of(context).push(themedRoute(
                   (_) => const AiCoachScreen(), name: 'AiCoachScreen')),
             ),
@@ -706,17 +733,17 @@ class ProfileScreen extends StatelessWidget {
             ),
           ),
           const _CardNote(
-              'Join the community, report bugs, or peek at the source.'),
+              'Tritt der Community bei, melde Bugs oder wirf einen Blick in den Quellcode.'),
 
           const SizedBox(height: Sp.x6),
 
           // ── About ────────────────────────────────────────────────────
-          const SectionHeader('About'),
+          const SectionHeader('Über'),
           _SettingsCard(rows: [
             ListRow(
               icon: OsIcon.activity,
-              title: 'About Edge',
-              subtitle: 'Affiliation, privacy policy, licenses',
+              title: 'Über CM Health',
+              subtitle: 'Zugehörigkeit, Datenschutzerklärung, Lizenzen',
               onTap: () => Navigator.of(context).push(
                   themedRoute((_) => const AboutScreen(), name: 'AboutScreen')),
             ),
@@ -725,12 +752,12 @@ class ProfileScreen extends StatelessWidget {
           const SizedBox(height: Sp.x6),
 
           // ── Reset ────────────────────────────────────────────────────
-          const SectionHeader('Reset'),
+          const SectionHeader('Zurücksetzen'),
           _SettingsCard(rows: [
             ListRow(
               icon: OsIcon.logout,
               iconColor: AppColors.critical,
-              title: 'Reset profile',
+              title: 'Profil zurücksetzen',
               onTap: () => _confirmSignOut(context, app),
             ),
           ]),
@@ -738,12 +765,12 @@ class ProfileScreen extends StatelessWidget {
           const SizedBox(height: Sp.x6),
 
           // ── Developer ────────────────────────────────────────────────
-          const SectionHeader('Developer'),
+          const SectionHeader('Entwickler'),
           _SettingsCard(rows: [
             ListRow(
               icon: OsIcon.edit,
-              title: 'Design gallery',
-              value: 'All components',
+              title: 'Design-Galerie',
+              value: 'Alle Komponenten',
               onTap: () => Navigator.of(context).push(themedRoute(
                   (_) => const DesignGalleryScreen(),
                   name: 'DesignGalleryScreen')),
@@ -763,7 +790,7 @@ class ProfileScreen extends StatelessWidget {
           children: [
             AppIcon(OsIcon.wear, size: 22, color: AppColors.inkMuted),
             const SizedBox(width: Sp.x3),
-            Expanded(child: Text('No strap paired.', style: AppText.bodySoft)),
+            Expanded(child: Text('Kein Band gekoppelt.', style: AppText.bodySoft)),
           ],
         ),
       );
@@ -774,19 +801,21 @@ class ProfileScreen extends StatelessWidget {
     final (statusTone, statusText) = switch (conn) {
       // Single listening mode — no separate "syncing" state; history + live
       // records both stream over the one link.
-      'connected' => (ChipTone.positive, 'Connected'),
-      'connecting' || 'scanning' => (ChipTone.warn, 'Connecting…'),
-      _ => (ChipTone.neutral, 'Disconnected'),
+      'connected' => (ChipTone.positive, 'Verbunden'),
+      'connecting' || 'scanning' => (ChipTone.warn, 'Verbinde…'),
+      _ => (ChipTone.neutral, 'Getrennt'),
     };
 
     return DeviceTile(
-      name: app.strapName ?? 'OpenStrap',
+      name: app.strapName ?? 'CM Health',
       statusText: statusText,
       statusTone: statusTone,
       battery: d.batteryPct == null
           ? '—'
           : '${d.batteryPct!.round()}%${d.charging == true ? ' ⚡' : ''}',
-      wrist: d.wristOn == null ? '—' : (d.wristOn! ? 'On wrist' : 'Off wrist'),
+      wrist: d.wristOn == null
+          ? '—'
+          : (d.wristOn! ? 'Am Handgelenk' : 'Nicht am Handgelenk'),
       serial: d.serial ?? app.paired?.serial ?? '—',
       // Manual pull: anything the strap flashed that we don't hold yet, over
       // the CURRENT connection (no reconnect). Only offered while connected.
@@ -808,8 +837,13 @@ class ProfileScreen extends StatelessWidget {
   }
 
   static String _sexLabel(String? s) {
-    if (s == null || s.isEmpty) return 'Add';
-    return s[0].toUpperCase() + s.substring(1);
+    if (s == null || s.isEmpty) return 'Hinzufügen';
+    return switch (s) {
+      'male' => 'Männlich',
+      'female' => 'Weiblich',
+      'other' => 'Divers',
+      _ => s[0].toUpperCase() + s.substring(1),
+    };
   }
 
   /// Compact host for the row value (drop scheme + trailing path).
@@ -834,15 +868,15 @@ class ProfileScreen extends StatelessWidget {
         shape: RoundedRectangleBorder(
           borderRadius: BorderRadius.circular(R.card),
         ),
-        title: Text('Companion URL', style: AppText.h2),
+        title: Text('Companion-URL', style: AppText.h2),
         content: Column(
           mainAxisSize: MainAxisSize.min,
           crossAxisAlignment: CrossAxisAlignment.start,
           children: [
             Text(
-              'The one backend the app connects to — app updates, announcements, '
-              'opt-in diagnostics and existing-user import. Leave blank to use '
-              'the build default.',
+              'Das eine Backend, mit dem sich die App verbindet — App-Updates, '
+              'Ankündigungen, Opt-in-Diagnosen und Import für Bestandsnutzer. '
+              'Leer lassen, um den Build-Standard zu verwenden.',
               style: AppText.captionMuted,
             ),
             const SizedBox(height: Sp.x3),
@@ -860,11 +894,11 @@ class ProfileScreen extends StatelessWidget {
         actions: [
           TextButton(
             onPressed: () => Navigator.of(dctx).pop(),
-            child: const Text('Cancel'),
+            child: const Text('Abbrechen'),
           ),
           FilledButton(
             onPressed: () => Navigator.of(dctx).pop(ctrl.text.trim()),
-            child: const Text('Save'),
+            child: const Text('Speichern'),
           ),
         ],
       ),
@@ -875,8 +909,8 @@ class ProfileScreen extends StatelessWidget {
       SnackBar(
         content: Text(
           url.isEmpty
-              ? 'Companion URL cleared — using the build default.'
-              : 'Companion URL saved.',
+              ? 'Companion-URL gelöscht — der Build-Standard wird verwendet.'
+              : 'Companion-URL gespeichert.',
         ),
       ),
     );
@@ -899,11 +933,11 @@ class ProfileScreen extends StatelessWidget {
   Future<void> _confirmSignOut(BuildContext context, AppState app) async {
     final ok = await _confirm(
       context,
-      title: 'Reset profile?',
+      title: 'Profil zurücksetzen?',
       body:
-          'Clears your local profile (name, age, body metrics) and forgets your '
-          'strap. Your stored band data stays on this phone.',
-      confirmLabel: 'Reset',
+          'Löscht dein lokales Profil (Name, Alter, Körperdaten) und vergisst '
+          'dein Band. Deine gespeicherten Band-Daten bleiben auf diesem Telefon.',
+      confirmLabel: 'Zurücksetzen',
       destructive: true,
     );
     if (ok == true) await app.signOut();
@@ -964,13 +998,13 @@ class _TaskerTokenRowState extends State<_TaskerTokenRow> {
           : () async {
               await Clipboard.setData(ClipboardData(text: token));
               if (context.mounted) {
-                _snack(context, 'Copied automation token.');
+                _snack(context, 'Automatisierungs-Token kopiert.');
               }
             },
       child: ListRow(
         icon: OsIcon.activity,
-        title: 'Automation token',
-        subtitle: token ?? 'Loading…',
+        title: 'Automatisierungs-Token',
+        subtitle: token ?? 'Lädt…',
       ),
     );
   }
@@ -1124,10 +1158,10 @@ class _DeviceTileState extends State<DeviceTile> {
             Row(
               children: [
                 Expanded(
-                  child: _fact(OsIcon.battery, 'Battery', widget.battery, tone),
+                  child: _fact(OsIcon.battery, 'Akku', widget.battery, tone),
                 ),
                 Expanded(
-                  child: _fact(OsIcon.heartRate, 'Wrist', widget.wrist, tone),
+                  child: _fact(OsIcon.heartRate, 'Handgelenk', widget.wrist, tone),
                 ),
               ],
             ),
@@ -1135,7 +1169,7 @@ class _DeviceTileState extends State<DeviceTile> {
             Row(
               children: [
                 Expanded(
-                  child: _fact(OsIcon.info, 'Serial', widget.serial, tone),
+                  child: _fact(OsIcon.info, 'Seriennummer', widget.serial, tone),
                 ),
                 if (widget.onSyncNow != null)
                   Pressable(
@@ -1165,7 +1199,7 @@ class _DeviceTileState extends State<DeviceTile> {
                             const SizedBox(width: Sp.x2),
                           ],
                           Text(
-                            _syncing ? 'Syncing…' : 'Sync now',
+                            _syncing ? 'Synchronisiere…' : 'Jetzt synchronisieren',
                             style: AppText.caption.copyWith(color: tone.fg),
                           ),
                         ],
@@ -1229,7 +1263,7 @@ Future<bool?> _confirm(
       actions: [
         TextButton(
           onPressed: () => Navigator.pop(c, false),
-          child: const Text('Cancel'),
+          child: const Text('Abbrechen'),
         ),
         FilledButton(
           style: destructive
@@ -1273,10 +1307,10 @@ class _HealthSection extends StatelessWidget {
                 child: Column(
                   crossAxisAlignment: CrossAxisAlignment.start,
                   children: [
-                    Text('Sync to $store', style: AppText.title),
+                    Text('Mit $store synchronisieren', style: AppText.title),
                     const SizedBox(height: 1),
                     Text(
-                      'Sleep, resting HR, HRV, respiratory rate, energy & workouts.',
+                      'Schlaf, Ruhepuls, HRV, Atemfrequenz, Energie & Workouts.',
                       style: AppText.captionMuted,
                     ),
                   ],
@@ -1317,12 +1351,12 @@ class _HealthSection extends StatelessWidget {
           child: Column(
             crossAxisAlignment: CrossAxisAlignment.start,
             children: [
-              const Text('Use phone step count'),
+              const Text('Schrittzahl des Telefons nutzen'),
               const SizedBox(height: 1),
               Text(
-                'Your band is on your wrist and can’t reliably count steps. '
-                'Your phone already counts them — read them from $store. '
-                'Stays on your device.',
+                'Dein Band sitzt am Handgelenk und kann Schritte nicht '
+                'zuverlässig zählen. Dein Telefon zählt sie bereits — sie '
+                'werden aus $store gelesen. Bleibt auf deinem Gerät.',
                 style: AppText.captionMuted,
               ),
               // The failure mode this exists for: on iOS the permission prompt
@@ -1350,7 +1384,7 @@ class _HealthSection extends StatelessWidget {
             final ok = await app.requestPhoneSteps();
             if (!ok && context.mounted) {
               ScaffoldMessenger.of(context).showSnackBar(
-                SnackBar(content: Text('$store didn’t grant step access.')),
+                SnackBar(content: Text('$store hat den Schrittzugriff nicht erlaubt.')),
               );
             }
           },
@@ -1362,18 +1396,20 @@ class _HealthSection extends StatelessWidget {
   /// One line telling the user whether the read is actually producing anything.
   static String _phoneStepsStatus(AppState app, String store) {
     final days = app.phoneStepsLastSyncedDays;
-    if (days == null) return 'Reading…';
+    if (days == null) return 'Lese…';
     if (days == 0) {
-      return 'No data from $store yet. If you never saw a permission prompt, '
-          'allow Steps for OpenStrap in $store settings.';
+      return 'Noch keine Daten aus $store. Falls du nie eine '
+          'Berechtigungsabfrage gesehen hast, erlaube Schritte für CM Health '
+          'in den $store-Einstellungen.';
     }
     final total = app.phoneStepsLastTotal ?? 0;
     if (total == 0) {
-      return 'Connected to $store — no steps recorded in the last $days day'
-          '${days == 1 ? '' : 's'}.';
+      return 'Mit $store verbunden — keine Schritte '
+          '${days == 1 ? 'am letzten Tag' : 'in den letzten $days Tagen'} '
+          'aufgezeichnet.';
     }
-    return 'Read $total steps from $store over $days day'
-        '${days == 1 ? '' : 's'}.';
+    return '$total Schritte aus $store über '
+        '${days == 1 ? '1 Tag' : '$days Tage'} gelesen.';
   }
 
   Widget _statusRow(BuildContext context, HealthLinkState st, String store) {
@@ -1381,21 +1417,21 @@ class _HealthSection extends StatelessWidget {
     // Health Connect must be installed/updated first (Android).
     if (st == HealthLinkState.notInstalled) {
       return _cta(
-        '$store isn’t installed. Install it, then come back.',
-        'Install',
+        '$store ist nicht installiert. Installiere es und komm dann zurück.',
+        'Installieren',
         () => app.installHealthConnect(),
       );
     }
     if (st == HealthLinkState.needsUpdate) {
       return _cta(
-        '$store needs an update before we can write to it.',
-        'Update',
+        '$store braucht ein Update, bevor wir hineinschreiben können.',
+        'Aktualisieren',
         () => app.installHealthConnect(),
       );
     }
     if (st == HealthLinkState.unsupported) {
       return Text(
-        '$store isn’t available on this device.',
+        '$store ist auf diesem Gerät nicht verfügbar.',
         style: AppText.captionMuted,
       );
     }
@@ -1404,10 +1440,12 @@ class _HealthSection extends StatelessWidget {
     // hides it), so we just keep writing and tell the user how to allow access
     // if data isn't showing — never a stuck "Grant access" gate.
     final subtitle = app.healthIsApple
-        ? 'New days are written automatically. If they don’t appear in Apple '
-              'Health, tap Allow access and turn ON every category.'
-        : 'New days are written automatically. If they don’t appear, open '
-              'Health Connect → App permissions → OpenStrap → Allow all.';
+        ? 'Neue Tage werden automatisch geschrieben. Wenn sie nicht in Apple '
+              'Health erscheinen, tippe auf „Zugriff erlauben" und schalte '
+              'jede Kategorie EIN.'
+        : 'Neue Tage werden automatisch geschrieben. Wenn sie nicht '
+              'erscheinen, öffne Health Connect → App-Berechtigungen → '
+              'CM Health → Alle erlauben.';
     return Column(
       crossAxisAlignment: CrossAxisAlignment.start,
       children: [
@@ -1416,7 +1454,8 @@ class _HealthSection extends StatelessWidget {
             AppIcon(OsIcon.check, size: 16, color: AppColors.positive),
             const SizedBox(width: Sp.x2),
             Expanded(
-              child: Text('Syncing to $store.', style: AppText.captionMuted),
+              child: Text('Synchronisierung mit $store aktiv.',
+                  style: AppText.captionMuted),
             ),
           ],
         ),
@@ -1435,7 +1474,7 @@ class _HealthSection extends StatelessWidget {
                 visualDensity: VisualDensity.compact,
                 minimumSize: const Size(0, 40),
               ),
-              child: const Text('Allow access'),
+              child: const Text('Zugriff erlauben'),
             ),
             if (!app.healthIsApple)
               OutlinedButton(
@@ -1444,7 +1483,7 @@ class _HealthSection extends StatelessWidget {
                   visualDensity: VisualDensity.compact,
                   minimumSize: const Size(0, 40),
                 ),
-                child: const Text('Open Health Connect'),
+                child: const Text('Health Connect öffnen'),
               ),
             TextButton(
               onPressed: () async {
@@ -1453,13 +1492,13 @@ class _HealthSection extends StatelessWidget {
                   SnackBar(
                     content: Text(
                       n > 0
-                          ? 'Synced $n day${n == 1 ? '' : 's'} to $store.'
-                          : 'Up to date — new days sync as they’re computed.',
+                          ? '$n Tag${n == 1 ? '' : 'e'} mit $store synchronisiert.'
+                          : 'Auf dem neuesten Stand — neue Tage werden nach der Berechnung synchronisiert.',
                     ),
                   ),
                 );
               },
-              child: const Text('Sync now'),
+              child: const Text('Jetzt synchronisieren'),
             ),
           ],
         ),
@@ -1528,9 +1567,9 @@ class _KeepAliveRowState extends State<_KeepAliveRow> {
         _SettingsCard(rows: [
           ListRow(
             icon: OsIcon.battery,
-            title: 'Keep alive in background',
-            subtitle: 'So overnight tracking isn’t frozen by the OS',
-            value: _exempt == null ? '…' : (_exempt! ? 'On' : 'Allow'),
+            title: 'Im Hintergrund aktiv halten',
+            subtitle: 'Damit das OS die nächtliche Aufzeichnung nicht einfriert',
+            value: _exempt == null ? '…' : (_exempt! ? 'An' : 'Erlauben'),
             trailing: _exempt == true
                 ? AppIcon(OsIcon.check, size: 18, color: AppColors.positive)
                 : null,
@@ -1548,9 +1587,10 @@ class _KeepAliveRowState extends State<_KeepAliveRow> {
           if (_showOemRow)
             ListRow(
               icon: OsIcon.battery,
-              title: 'Allow auto-start',
-              subtitle: 'Your phone maker needs one more step to keep '
-                  'syncing overnight — tap to open it',
+              title: 'Autostart erlauben',
+              subtitle: 'Dein Telefonhersteller braucht noch einen Schritt, '
+                  'damit die Synchronisierung über Nacht weiterläuft — tippe, '
+                  'um ihn zu öffnen',
               onTap: () => context.read<AppState>().openOemAutostartSettings(),
             ),
         ]),
@@ -1579,22 +1619,22 @@ class _ForceQuitInfoRow extends StatelessWidget {
         _SettingsCard(rows: [
           ListRow(
             icon: OsIcon.battery,
-            title: 'Force-quitting pauses background sync',
-            subtitle: 'If you swipe OpenStrap away, reopen it once to resume',
+            title: 'Erzwungenes Beenden pausiert die Hintergrund-Synchronisierung',
+            subtitle: 'Wenn du CM Health wegwischst, öffne es einmal neu, um fortzufahren',
             onTap: () => showInfoSheet(
               context,
-              title: 'Force-quitting pauses background sync',
-              body: 'Swiping OpenStrap away in the app switcher tells iOS to '
-                  'stop it completely — including background sync. This is '
-                  'an iOS rule that applies to every app, not something '
-                  'OpenStrap can change.',
+              title: 'Erzwungenes Beenden pausiert die Hintergrund-Synchronisierung',
+              body: 'Wenn du CM Health im App-Umschalter wegwischst, stoppt '
+                  'iOS die App vollständig — inklusive '
+                  'Hintergrund-Synchronisierung. Das ist eine iOS-Regel für '
+                  'jede App und nichts, was CM Health ändern kann.',
               bullets: const [
-                'Your data is never lost — the band keeps recording, and '
-                    'the next sync catches everything up.',
-                'To resume background syncing, just open OpenStrap once — '
-                    'you don\'t need to do anything else.',
-                'If you get a "hasn\'t synced in a while" notification, '
-                    'this is usually why.',
+                'Deine Daten gehen nie verloren — das Band zeichnet weiter '
+                    'auf, und die nächste Synchronisierung holt alles nach.',
+                'Um die Hintergrund-Synchronisierung fortzusetzen, öffne '
+                    'CM Health einfach einmal — mehr ist nicht nötig.',
+                'Wenn du eine Meldung bekommst, dass länger nicht '
+                    'synchronisiert wurde, ist das meist der Grund.',
               ],
             ),
           ),
@@ -1616,29 +1656,28 @@ class _DeviceSheet extends StatelessWidget {
     // pass here missed `device`/`paired` — re-audited against every `live.`
     // touchpoint in this class after finding the same gap cost a real bug in
     // the main ProfileScreen build above.)
-    context.select<AppState, (bool, int?, String?, dynamic, dynamic)>(
-      (a) => (a.isConnected, a.alarmEpoch, a.strapName, a.device, a.paired),
+    context.select<AppState, (bool, String?, dynamic, dynamic)>(
+      (a) => (a.isConnected, a.strapName, a.device, a.paired),
     );
     final live = context.read<AppState>();
     final connected = live.isConnected;
-    final alarm = live.alarmEpoch;
 
     return _SheetShell(
-      title: live.strapName ?? 'OpenStrap',
+      title: live.strapName ?? 'CM Health',
       children: [
         if (!connected)
-          _Notice('Connect to your strap to rename it or change the alarm.'),
+          _Notice('Verbinde dein Band, um es umzubenennen oder zu testen.'),
         ListRow(
           icon: OsIcon.edit,
-          title: 'Rename strap',
-          value: live.strapName ?? 'OpenStrap',
+          title: 'Band umbenennen',
+          value: live.strapName ?? 'CM Health',
           divider: true,
           onTap: connected ? () => _rename(context, live) : null,
         ),
         ListRow(
           icon: OsIcon.bluetooth,
-          title: 'Find my strap',
-          value: connected ? 'Buzz and track' : 'Needs a connection',
+          title: 'Band finden',
+          value: connected ? 'Vibrieren und orten' : 'Braucht eine Verbindung',
           divider: true,
           onTap: connected
               ? () {
@@ -1651,67 +1690,20 @@ class _DeviceSheet extends StatelessWidget {
                 }
               : null,
         ),
-        ListRow(
-          icon: OsIcon.alarm,
-          title: 'Smart alarm',
-          value: alarm != null ? _fmtAlarm(alarm) : 'Off',
-          onTap: connected ? () => _setAlarm(context, live) : null,
-        ),
-        // ALARM STATUS: the rich firing form (haptic waveform) is wired, and the
-        // strap now CONFIRMS the alarm latched via its own event stream — so this
-        // caption reflects the real confirmation state instead of a blanket
-        // "experimental" warning.
-        if (alarm != null)
-          Padding(
-            padding: const EdgeInsets.only(top: Sp.x1),
-            child: Text(
-              _alarmStatusText(live),
-              style: AppText.captionMuted,
-            ),
-          ),
-        if (connected && alarm != null)
-          Padding(
-            padding: const EdgeInsets.only(top: Sp.x2),
-            child: Row(children: [
-              OutlinedButton.icon(
-                onPressed: () async {
-                  try {
-                    await live.testAlarmBuzz();
-                    if (context.mounted) _snack(context, 'Sent a test buzz.');
-                  } catch (e) {
-                    if (context.mounted) _snack(context, 'Buzz failed: $e');
-                  }
-                },
-                icon: const AppIcon(OsIcon.notifications, size: 18),
-                label: const Text('Test buzz'),
-              ),
-              const SizedBox(width: Sp.x2),
-              OutlinedButton.icon(
-                onPressed: () async {
-                  try {
-                    await live.clearAlarm();
-                    if (context.mounted) {
-                      Navigator.pop(context);
-                      _snack(context, 'Alarm cleared.');
-                    }
-                  } catch (e) {
-                    if (context.mounted) _snack(context, 'Clear failed: $e');
-                  }
-                },
-                icon: const AppIcon(OsIcon.cancel, size: 18),
-                label: const Text('Clear alarm'),
-              ),
-            ]),
-          ),
+        // CM Health v2.1.0: Der band-interne Smart-Wecker ist abgeschafft — der
+        // Wecker lebt jetzt als Handy-Wecker-Engine mit eigenem Menüpunkt
+        // (Today-Kopfzeile), weil diese Band-Firmware gestellte Wecker nie
+        // zuverlässig gefeuert hat. Die Vibrationsmuster-Reihe unten bleibt
+        // als Direkt-Test der drei wirksamen Effekte.
         if (connected) ...[
           const SizedBox(height: Sp.x2),
-          Text('Buzz patterns', style: AppText.captionMuted),
+          Text('Vibrationsmuster', style: AppText.captionMuted),
           const SizedBox(height: Sp.x2),
           Wrap(
             spacing: Sp.x2,
             runSpacing: Sp.x2,
             children: [
-              for (final p in [0, 1, 2, 3, 4, 5, 6, 7])
+              for (final p in [0, 1, 2]) // nur 0–2 sind auf dieser Firmware wirksam
                 SizedBox(
                   width: 48,
                   height: 48,
@@ -1720,11 +1712,11 @@ class _DeviceSheet extends StatelessWidget {
                       try {
                         await live.testBuzzPattern(p);
                         if (context.mounted) {
-                          _snack(context, 'Pattern $p sent.');
+                          _snack(context, 'Muster $p gesendet.');
                         }
                       } catch (e) {
                         if (context.mounted) {
-                          _snack(context, 'Pattern $p failed: $e');
+                          _snack(context, 'Muster $p fehlgeschlagen: $e');
                         }
                       }
                     },
@@ -1740,7 +1732,7 @@ class _DeviceSheet extends StatelessWidget {
         Divider(height: Sp.x4, thickness: 1, color: AppColors.divider),
         ListRow(
           icon: OsIcon.info,
-          title: 'Serial',
+          title: 'Seriennummer',
           value: live.device.serial ?? live.paired?.serial ?? '—',
         ),
         const SizedBox(height: Sp.x4),
@@ -1756,20 +1748,13 @@ class _DeviceSheet extends StatelessWidget {
             ),
             onPressed: () => _forget(context, live),
             icon: AppIcon(OsIcon.cancel, size: 18, color: AppColors.critical),
-            label: const Text('Forget device'),
+            label: const Text('Gerät vergessen'),
           ),
         ),
       ],
     );
   }
 
-  String _fmtAlarm(int epoch) {
-    final dt = DateTime.fromMillisecondsSinceEpoch(epoch * 1000).toLocal();
-    final h = dt.hour.toString().padLeft(2, '0');
-    final m = dt.minute.toString().padLeft(2, '0');
-    return '$h:$m  (${dt.month}/${dt.day})';
-  }
-
   Future<void> _rename(BuildContext context, AppState app) async {
     final ctrl = TextEditingController(text: app.strapName ?? '');
     final name = await showModalBottomSheet<String>(
@@ -1780,20 +1765,20 @@ class _DeviceSheet extends StatelessWidget {
         borderRadius: BorderRadius.vertical(top: Radius.circular(R.card)),
       ),
       builder: (_) => _SheetShell(
-        title: 'Rename strap',
+        title: 'Band umbenennen',
         children: [
           TextField(
             controller: ctrl,
             maxLength: 20,
             autofocus: true,
-            decoration: const InputDecoration(labelText: 'Strap name'),
+            decoration: const InputDecoration(labelText: 'Band-Name'),
           ),
           const SizedBox(height: Sp.x4),
           SizedBox(
             width: double.infinity,
             child: FilledButton(
               onPressed: () => Navigator.pop(context, ctrl.text.trim()),
-              child: const Text('Save'),
+              child: const Text('Speichern'),
             ),
           ),
         ],
@@ -1802,64 +1787,27 @@ class _DeviceSheet extends StatelessWidget {
     if (name == null || name.isEmpty) return;
     try {
       await app.renameStrap(name);
-      if (context.mounted) _snack(context, 'Renamed to "$name".');
-    } catch (e) {
-      if (context.mounted) _snack(context, 'Rename failed: $e');
-    }
-  }
-
-  Future<void> _setAlarm(BuildContext context, AppState app) async {
-    final now = DateTime.now();
-    final picked = await showTimePicker(
-      context: context,
-      initialTime: TimeOfDay(hour: (now.hour + 8) % 24, minute: 0),
-    );
-    if (picked == null) return;
-    var when = DateTime(
-      now.year,
-      now.month,
-      now.day,
-      picked.hour,
-      picked.minute,
-    );
-    if (!when.isAfter(now)) when = when.add(const Duration(days: 1));
-    try {
-      await app.setAlarm(when);
-      if (context.mounted) {
-        _snack(
-          context,
-          'Alarm set for ${picked.format(context)} — confirming with the strap…',
-        );
-      }
+      if (context.mounted) _snack(context, 'Umbenannt in "$name".');
     } catch (e) {
-      if (context.mounted) _snack(context, 'Set failed: $e');
+      if (context.mounted) _snack(context, 'Umbenennen fehlgeschlagen: $e');
     }
   }
 
-  /// Live alarm caption driven by the strap's confirmation events (see AppState's
-  /// alarm state machine): confirmed ✓ / pending / soft unconfirmed warning.
-  String _alarmStatusText(AppState app) {
-    if (app.alarmConfirmed) return 'Alarm set ✓ — confirmed by your strap.';
-    if (app.alarmPending) return 'Setting alarm… waiting for the strap to confirm.';
-    return 'Alarm sent, but the strap hasn\'t confirmed it yet. '
-        'Tap "Test buzz" to check it fires, and keep a backup alarm.';
-  }
-
   Future<void> _forget(BuildContext context, AppState app) async {
     final ok = await _confirm(
       context,
-      title: 'Forget this device?',
+      title: 'Dieses Gerät vergessen?',
       body:
-          'You\'ll need to re-pair your strap to sync again. Your stored data '
-          'stays on this phone.',
-      confirmLabel: 'Forget',
+          'Du musst dein Band neu koppeln, um wieder zu synchronisieren. Deine '
+          'gespeicherten Daten bleiben auf diesem Telefon.',
+      confirmLabel: 'Vergessen',
       destructive: true,
     );
     if (ok != true) return;
     await app.unpair();
     if (context.mounted) {
       Navigator.pop(context);
-      _snack(context, 'Device forgotten.');
+      _snack(context, 'Gerät vergessen.');
     }
   }
 }
@@ -1920,12 +1868,12 @@ class _ProfileEditSheetState extends State<_ProfileEditSheet> {
       });
       if (mounted) {
         Navigator.pop(context);
-        _snack(context, 'Profile saved.');
+        _snack(context, 'Profil gespeichert.');
       }
     } catch (e) {
       if (mounted) {
         setState(() => _saving = false);
-        _snack(context, 'Save failed: $e');
+        _snack(context, 'Speichern fehlgeschlagen: $e');
       }
     }
   }
@@ -1933,7 +1881,7 @@ class _ProfileEditSheetState extends State<_ProfileEditSheet> {
   @override
   Widget build(BuildContext context) {
     return _SheetShell(
-      title: 'Edit profile',
+      title: 'Profil bearbeiten',
       children: [
         TextField(
           controller: _name,
@@ -1946,7 +1894,7 @@ class _ProfileEditSheetState extends State<_ProfileEditSheet> {
               child: TextField(
                 controller: _age,
                 keyboardType: TextInputType.number,
-                decoration: const InputDecoration(labelText: 'Age'),
+                decoration: const InputDecoration(labelText: 'Alter'),
               ),
             ),
             const SizedBox(width: Sp.x3),
@@ -1968,7 +1916,7 @@ class _ProfileEditSheetState extends State<_ProfileEditSheet> {
           ],
         ),
         const SizedBox(height: Sp.x4),
-        Text('SEX (OPTIONAL)',
+        Text('GESCHLECHT (OPTIONAL)',
             style: AppText.overline.copyWith(color: AppColors.inkMuted)),
         const SizedBox(height: Sp.x2),
         Wrap(
@@ -1976,14 +1924,18 @@ class _ProfileEditSheetState extends State<_ProfileEditSheet> {
           children: [
             for (final opt in const ['male', 'female', 'other'])
               ToggleChip(
-                opt[0].toUpperCase() + opt.substring(1),
+                switch (opt) {
+                  'male' => 'Männlich',
+                  'female' => 'Weiblich',
+                  _ => 'Divers',
+                },
                 selected: _sex == opt,
                 onTap: () => setState(() => _sex = _sex == opt ? null : opt),
               ),
           ],
         ),
         const SizedBox(height: Sp.x2),
-        Text('Improves your calorie estimate.', style: AppText.captionMuted),
+        Text('Verbessert deine Kalorienschätzung.', style: AppText.captionMuted),
         const SizedBox(height: Sp.x5),
         SizedBox(
           width: double.infinity,
@@ -1998,7 +1950,7 @@ class _ProfileEditSheetState extends State<_ProfileEditSheet> {
                       color: Colors.white,
                     ),
                   )
-                : const Text('Save profile'),
+                : const Text('Profil speichern'),
           ),
         ),
       ],
